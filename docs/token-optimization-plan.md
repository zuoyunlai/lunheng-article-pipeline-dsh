# Token 优化计划（token-optimization-plan）

> 状态：**草案 / 待 token 实测确认优先级**｜创建：2026-09-08｜仓库：lunheng-article-pipeline-dsh
> 说明：以下收益为量级判断；正式动手前先跑 `node scripts/token-cost.mjs` 取各 Phase/角色的实测分布，再按数据落地。

## 目标
在保持论衡“证据底座 + 三角验证 + 独立审计”质量的前提下，降低每次会话/子代理的 cacheRead 与 token 成本（仓库记录基线：191M → 80-90M，本轮目标再降 30-50%）。

## 实测步骤（先做）
1. 用一篇 3000-5000 字论文全跑一轮，逐 Phase 记录 `token-cost.mjs --sessions <主会话>,<子代理…>`；
2. 产出各 Phase/角色 token 分布表 → 更新本计划“实测数据”节；
3. 按分布从下表中取 Top 3 立项。

## 优化清单（按性价比）

### 🔴 P1 高性价比
1. **技能本体瘦身**
   - 现状：SKILL.md ≈33KB 每次加载注入；frontmatter description 冗长（每次列技能目录都占 token）；00-主控-扩展职责 41KB 等大文档常被整读。
   - 动作：SKILL.md 拆「入口版（速查+目录+引用）」，机制大段移入 `references/*` 按需 read；description 压缩至 ~120 字；重复段落以“单一真源 + 引用链接”替代。
   - 落点：SKILL.md / AGENTS.md / 相关 references；consistency-check 保证不漂移。
2. **子代理开工卡（prompt 瘦身）**
   - 现状：每次 spawn 携带长自包含 prompt + 子代理先整读角色卡（20-40KB）。
   - 动作：T1-T9 各产 1-2KB「开工卡」（任务/输出路径/必守铁律索引/格式样例链接）；全文按需 read 小节。
3. **共享审计视图（消除重复全读）**
   - 现状：T4/T5/T6/T7/T9/G14 各自重复读 稿+三卡+报告。
   - 动作：闸门处生成共享精简视图（build-evidence-bundle --summary 已有雏形），角色只读视图 + 编号定位原文；派发模板写明“禁全文通读”，T8 复核。

### 🟡 P2 中性价比
4. **交接回报精简**：交接只回「摘要 ≤10 行 + 产物路径 + 关闭状态表」，报告全文落盘不粘贴。
5. **修订轮一律段级 diff**：v1 后默认 T5 段级 diff（≤25 步），主控机械编辑；T5 只读 §11 精简段写为硬约束。
6. **检索上下文累积控制**：每 ~10 次搜索本地小结替换原文；候选 URL 批量化（先清单再一次 web_fetch）。
7. **判定脚本化前移**：M-Form-8 等三角验证改为结构化清单供脚本/主控 JSON 判定，T8 不整读 46KB 算法文档。

### 🟢 P3 锦上添花
8. **token 可观测**（**v2.5.2-dsh.15：CLI 侧已落地**）——`token-cost.mjs --top N` 现在真的会输出 cacheRead/成本排名（旧版只有注释、代码是死变量）；「Phase 结尾自动记 token-cost + 每角色预算告警」仍未自动化，主控需在终检时手动跑（交付说明必填）。
9. **断线重派零重读**：中断重派只喂「状态文件 + 产物路径」，禁止重读全套素材（dsh.10 交接状态四要素已就位，落地为强制约定）。

## 修订状态记录
- [x] 2026-09-08：本计划文档创建；
- [x] 2026-09-08：SKILL.md frontmatter description 压缩（技能目录/加载即时减负）；
- [x] 2026-09-08：P1-1 技能本体拆分——SKILL.md 重写为「入口精简版」（保留全部 H2 锚点与核心规则，机制细节移引 references），33.0KB → 16.9KB（≈−49%），consistency exit 0；
- [x] 2026-09-08：P1-3 + P2-4 + P2-5 落地——pipeline-readme 新增「共享读取纪律」：审计视图优先（禁全文通读）、交接回报 ≤10 行、v1 后修订轮默认段级 diff；
- [ ] token 实测一轮并回填分布；
- [ ] P1-2/P1-3 开工卡与审计视图（待实测确认后执行）；
- [ ] P2/P3 项按优先级排期。
- [x] 2026-09-11（v2.5.2-dsh.15）：**审计视图真正可用**——旧版脚本把视图源写死 `final/定稿.md`，而定稿 Phase 5 才产出，导致 T6(3.6)/T7(4)/T9(4.5) 全部「被要求先读却无视图可读」；现改为 `--source` ＞ 定稿 ＞ `drafts/` 最高版本 三级回退，无正文时出素材阶段视图，且**主控在 Phase 2/3.6/4/4.5/5 派发前刷新**（闸门动作，不再只是 T8 的动作）。
- [x] 2026-09-11（v2.5.2-dsh.15）：**检索侧补上成功路径的收敛规则**——首轮软预算 ≤40 步 + 「连续 2 轮无新增卡即饱和」判据（此前全技能 `饱和/早停` 命中 0 处）。
- [x] 2026-09-11（v2.5.2-dsh.15）：**`token-cost --top N` 实装** + 无出处百分比断言清扫（新增一致性规则 ⑰，防「省 N%」自我繁殖）。
- [x] 2026-09-11（v2.5.2-dsh.16）：**SVG 图件链路补上机械门**——新增 M 门 **M-Form-9 图件闭环**（[图N] ↔ `final/图件/` ↔ 图上数字，缺图 P1·全缺 P0、启发式项 P2）+ 图件随证据包收齐 + 审计视图「图件对账」段；`md2html.mjs` 修三处（多图配图 `--fig-dir`、行内图位、坏 SVG 拒绝导出）；一致性规则 ⑥b 改为从脚本派生 + 新增 ⑱（图件口径）。**这也是「token 优化不再空转」的延伸**：图件对账让 T7/T8 不必逐文件翻图。

## 实测数据（test-paper-02，2026-09-09，dsh.10 首轮）

> 数据源：投影缓存 `session_projcache/sessions/<id>.json` 的 `tokenUsage.totals`（`token-cost.mjs` 旧布局失效，见下方工具问题）。

| 角色 | 会话数 | input | output | cacheRead |
|---|---|---|---|---|
| T1 文献 | 2 | 62,634 | 32,238 | 537,600 |
| T2 数据 | 2 | 85,065 | 37,065 | 1,048,320 |
| T3 案例 | 2 | 73,282 | 34,046 | 962,688 |
| T4 分析 | 1 | 72,143 | 48,969 | 1,336,320 |
| T5 写作 | 3 | 348,191 | 511,736 | 28,928,384 |
| T6 批判 | 1 | 96,947 | 48,411 | 1,333,760 |
| T7 审计 | 2 | 177,559 | 114,595 | 3,347,584 |
| T9 评审 | 1 | 66,079 | 19,188 | 395,008 |
| G14 检测 | 1 | 38,247 | 22,655 | 347,776 |
| **合计（子代理）** | 15 | **1,020,147** | **868,903** | **38,237,440** |

**解读与结论**：
1. **T5 是最大成本源**（cacheRead ≈28.9M，占子代理合计 76%）——且其 3 个会话含一次整稿重写与一次停滞尝试；强支持「v1 后默认段级 diff + 写手只读 §11 精简段」方向。
2. 检索 T1-T3 各 2 会话含首轮后台停滞/重跑，说明「后台子代理停滞→重派」带来 ~30-40% 检索侧冗余成本（韧性重派应更早触发）。
3. 主会话另行累计 input≈3.0M / cacheRead≈117.6M——主体为本超长会话的轮次/工具往返，与论文单次运行关系弱，未计入论文口径。
4. 瘦身后 SKILL（33→16.9KB）与「读取纪律」已在本轮实际执行；精确的“同题对照降幅”需在 dsh.10 优化与旧版间做同题 A/B（待后续轮）。

**工具适配（2026-09-09 已修，未发布）**：
- `token-cost.mjs`：兼容新版目录式投影缓存（`storages/session_projcache/sessions/*.json`，保留旧单文件回退）并清理 `topMode` 死代码 —— 实测与手工读数一致；
- `m-gate-check.mjs`：修复 M-Form-4/5 子串误报（`自主控制`/`板卡级别` 用负向前瞻）与 M-Form-8 编号章节标题豁免（去 `七、` 前缀后比对，结论/展望入豁免表）；
- `final-check.mjs`：依赖上述两脚本 + `final/定稿.md` 与 `final/证据包/`（build-evidence-bundle 生成），test-paper-02 复跑 **exit 0**；
- 附带约定：任务简报需含 `子问题 A/B/…` 与 `需找数据点 ≥N 条`（M-Integrity-1 机器可读）。

## 验收标准
- 实测：同主题论文总 cacheRead/token 较优化前降幅可量化；
- 质量：T7 审计仍 exit 0、G14 Pass、正文/引用闭环无回归；
- 一致性：`consistency-check.mjs` exit 0。
