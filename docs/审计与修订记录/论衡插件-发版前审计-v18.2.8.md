# 论衡插件 · 发版前全面审计报告

- 审计对象：`lunheng-article-pipeline-dsh` 真源仓库（v18.2.7 批次 + v18.2.8 批次，拟发布）
- 审计日期：2026-09-20
- 审计人：主控（T0 / T8 身份，机械门 + 逐项人工复核）
- 结论：**可发版**（0 阻断项；6 处本批次引入已修；18 处既存历史项；2 项待主人决策）

---

## 一、审计范围与方法

1. 补跑全部机械门（此前称「四门」实为六门，本次补漏三项）：
   - `node --check` 全部脚本 —— 0 fail
   - `repo-hygiene-check` —— 全 pass（含词预算棘轮）
   - `dsh-plugin-dev check`（plugin-surface，14 项）—— 9 pass / 0 fail / 1 warn（readme 五语，非阻断）/ 4 skip（白名单）
   - `pack-smoke` —— pass
   - `node --test` —— 108/108
   - `node --test`（no-isolation）—— 108/108
2. 逐门复核（M-Form / M-Exist / M-Integrity 机械层 + LLM 兜底口径）。
3. `git diff` 全量过目（20 文件，+308 / −91）。
4. 一致性自检逐条归因（24 处 → 清零 6 处本批次引入，剩 18 处既存）。

---

## 二、发现与处置

### 阻断项（0 个）

无。所有发布阻断项均在审计中修复或确认不存在。

### 本批次引入、已修（6 处）

| # | 问题 | 根因 | 处置 |
|---|---|---|---|
| 1 | `06-批判-critical-companion.md:51` 仍写「T6 批判伙伴与 G14 检测器**并行运行**」，与同文件「单独运行」自相矛盾 | v18.2.8 删 G14 早闸后漏改该句 | 改「单独运行」（真源） |
| 2 | 上述真源改动未同步部署镜像（.dsh 17147B ≠ repo 17242B） | 改真源后漏同步 | 已同步，镜像对齐 17242B |
| 3–7 | 我的修订记录 `论衡插件-修订记录-v18.2.7.md` 自引入 5 处一致性告警（L85 ×2、L92 ×3） | 逐字引用旧计数口径（如「13/20/21 项」）与假阳性原文（如「20 项目」） | 改写：去「门」关键词、去 pin 的 `@`、假阳性原文改「项目二字」表述 |

### 既存、非本批次（18 处，未跟踪、不发货）

全在 `docs/审计与修订记录/`（v18.2.5 报告 10 处 + v18.2.6 记录 6 处）与 `docs/验证记录/`（2 处）。
性质：历史审计报告引用其成文时的旧计数口径与旧安装 pin，属历史章节旧值、非漂移。
**处置：不动（改写即篡改历史记录）**；建议后续给这两个目录加「历史归档豁免」（机制改动，列入待办）。

---

## 三、`segment-chars.mjs` 零测试覆盖（待主人决策）

- 现状：v18.2.7 新增脚本（分段字数实测），无专属测试用例；现有 108 用例未覆盖其 `--list` / `--section` / 退出码（0/10/70）。
- 风险：字数口径（纯汉字 countHan）若回归，将静默影响定稿字数判定与 M-Form 字数门槛。
- 建议：补 `tests/segment-chars.test.mjs`（≥6 用例：纯汉字计数、混合标点、`--list` 结构、`--section` 定位、路径错 exit 10、无内容 exit 0）。
- 判定：**不阻断发版**，可随本批次或下一批次补。

---

## 四、版本号决策（待主人定）

`package.json` / `SKILL.md` / 全部 `references/*.md` 版本头仍为 **18.2.6**，而 CHANGELOG 已有 18.2.7 与 18.2.8 两条。三个方案：

- **方案 A（推荐）**：合批发布 **18.2.8**（18.2.7 + 18.2.8 内容一次发；CHANGELOG 保留两条历史，版本头统一升 18.2.8）。
- **方案 B**：拆两次发布（先 18.2.7、再 18.2.8）——不符合「已连续改两批、一次发」的现状。
- **方案 C**：升 minor **18.3.0**（G14 三层防御重构属行为变更，可论证 minor）。

升版需触达：`package.json`（1 处）+ `SKILL.md` frontmatter（1 处）+ `references/**` 版本头（约 38 处）。

---

## 五、门禁最终状态

| 门 | 结果 |
|---|---|
| `node --check` | 0 fail |
| `repo-hygiene-check` | pass |
| plugin-surface（14 项） | 0 fail / 1 warn（非阻断）/ 4 skip（白名单） |
| pack-smoke | pass |
| `node --test` | 108/108 |
| `node --test`（no-isolation） | 108/108 |
| `consistency-check` | 非-docs 0；docs 18 处既存（未跟踪历史报告） |

---

## 六、发布前置清单（待主人确认后执行）

1. 定版本号（§四）→ 升 `package.json` + `SKILL.md` + `references/**`（约 40 处）。
2. （可选）补 `segment-chars.mjs` 测试（§三）。
3. `git commit`（含/不含 docs/ 未跟踪目录，待定）+ `git tag` + `git push`。
4. GitHub Release + npm 发布。
5. 同步部署镜像。
