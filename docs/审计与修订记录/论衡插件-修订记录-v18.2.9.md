# 论衡插件 · 修订记录 v18.2.9（第三方审计第 1 批：一致性止血）

- 修订日期：2026-09-21
- 授权依据：主人显式授权（「请直接帮我开始修订」）——按 `AGENTS.md` 「主人显式授权可改机制文件」唯一例外条款执行
- 审计来源：`E:\HERNESS\论衡插件-第三方全量审计报告-v18.2.8.md`（第三方独立审计，总分 7.0/10）
- 本批范围：审计 12 项重大中的 A1-A6（内容层）+ A8/A9/A10（脚本层）+ 2 项轻微；A7（参数解析统一）、A11（真实 Loader CI）、A12（T8 独立复核）及全部中等问题留第 2/3 批

## 一、安全流程执行记录

| 步骤 | 执行情况 |
|---|---|
| ① 改前备份 | `C:\Users\Zuoyunlai\.dsh\_backup\lunheng-auditfix-batch1-20260921-100253\`（13 文件，含行数与 SHA256 前 12 位基线，见备份时控制台输出） |
| ② 改中 | 全部 `edit` 工具精确匹配，未用 sed；06 卡两轮精简以回到词预算棘轮内 |
| ③ 改后验证 | `node --check`（4 脚本）✓；`repo-hygiene-check` ✓ 全绿；`node --test` **114/114**；`consistency-check` 非 docs **0**（18 处既存历史报告项，发版前审计已定性「不动」） |
| ④ 可回滚 | 备份保留；回滚命令见下 §四 |
| ⑤ 如实标注 | CHANGELOG `## 18.2.9` 条目 + 本记录 |

## 二、逐项改动清单

### 内容层（8 文件）

| 项 | 文件 | 改动 |
|---|---|---|
| A1 | `README.md` | 「八角色」→九角色；G14 并行对象 T6→T9（终闸三层防御）；「Phase 4.2 修订」上移为「Phase 4 审计打回」+ 双轨口径；配图行标注与审稿同段 |
| A2 | `QUICKSTART.md`、`references/agents/00-主控-coordinator.md` | 删除写死的旧版脚本计数数字，改纯指针（真源 = SKILL.md 白名单行；计数由 hygiene 门 ⑥ 从磁盘派生对账） |
| A3 | `references/agents/09-审稿-peer-reviewer.md` | T9 默认开关对齐 00-主控-扩展 §九：所有模式（含公众号）默认选中、学术必选；标注 v2.4.6 旧口径废止 |
| A4 | `references/agents/05-写作-writer.md` | 段级 diff 自检：现况字数由主控 `segment-chars.mjs --section` 前置实测、写手只估修改后侧、主控机检对账 |
| A5 | `references/agents/06/07/09 卡` | 余额闸门改可执行形态：仅当主人投喂余额/计费数据时执行，无数据时跳过并标注「余额未校验」 |
| A6 | `README.md`、`references/pipeline-readme.md` | 「≤2 轮」补 A 轨/B 轨双轨限定 |
| 轻 | `05/06 卡` | 悬空 `SOUL.md` 引用改指 `_shared/failure-modes.md` |

### 脚本层（4 文件 + 2 配套）

| 项 | 文件 | 改动 |
|---|---|---|
| A8 | `scripts/m-gate-check.mjs` | `endnoteScanText` 与 `secNames` 两处旧式 `^##\s+` 正则改走 `h2Headings` 真源（全角空格标题此前致「AI 使用声明」豁免失配 → 假 P0） |
| A9 | `scripts/_lib/exit-guard.mjs` | 补 `unhandledRejection` 兜底（fs 类 → 10，其余 → 70），消除「顶层 await fetch reject → exit 1 被读成 P1」撞码 |
| A10 | `scripts/token-budget.mjs` | 「项目路径不存在」exit 2 → 10；配套同步 `repo-hygiene-check.mjs` EXIT_CONTRACT、`docs/troubleshooting.md §8`、`tests/scripts.test.mjs` 断言 |
| 轻 | `scripts/m-gate-check.mjs`、`scripts/build-evidence-bundle.mjs` | `latestReport`/`latestVersioned` 前缀入正则前转义 |

### 镜像同步

`.dsh/skills/lunheng-article-pipeline/` 12 个技能侧文件已同步，MD5 双向核验不一致 0（`repo-hygiene-check.mjs`、`tests/`、`docs/`、`CHANGELOG.md` 为仓库侧文件，不进镜像）。

## 三、验证结果

- `node --check`：全部改动脚本通过（含 _lib 依赖）
- `repo-hygiene-check`：**全绿**（06 卡经两轮精简回到 17408B 棘轮内，未抬上限）
- `node --test "tests/**/*.test.mjs"`：**114/114**（含 token-budget 新 exit 10 断言）
- `consistency-check`：非 docs **0**；既存 18 处历史报告项不动（改写即篡改历史记录）；.dsh 同步告警全部清零

## 四、回滚命令（如需）

```powershell
$bk = 'C:\Users\Zuoyunlai\.dsh\_backup\lunheng-auditfix-batch1-20260921-100253'
$repo = 'E:\HERNESS\lunheng-article-pipeline-dsh'
Copy-Item "$bk\README.md","$bk\QUICKSTART.md","$bk\SKILL.md" "$repo\skills\lunheng-article-pipeline\" -Force
Copy-Item "$bk\*.md" "$repo\skills\lunheng-article-pipeline\references\agents\" -Force   # 6 张角色卡（文件名即对应）
Copy-Item "$bk\m-gate-check.mjs","$bk\token-budget.mjs","$bk\build-evidence-bundle.mjs" "$repo\skills\lunheng-article-pipeline\scripts\" -Force
Copy-Item "$bk\exit-guard.mjs" "$repo\skills\lunheng-article-pipeline\scripts\_lib\" -Force
# 回滚后重跑四门 + 重新同步镜像
```

> 注：`repo-hygiene-check.mjs`、`tests/scripts.test.mjs`、`docs/troubleshooting.md`、`CHANGELOG.md` 的配套改动未包含在上述备份集内（当次备份只覆盖 13 个技能侧文件）；如需整体回滚，用 git（本批改动尚未提交，`git checkout -- <file>` 即可）。

## 五、第 2 批（同日追加）：契约收口

> 备份：`C:\Users\Zuoyunlai\.dsh\_backup\lunheng-auditfix-batch2-20260921-144338\`（十文件）。验证口径与第 1 批相同。

| 项 | 文件 | 改动 |
|---|---|---|
| A7 | `m-gate-check / final-check / token-cost / token-budget / model-routing` | 参数解析全部迁移到 `_lib/cli-args.mjs` 唯一实现——m-gate-check 的未知旗标/多余位置参数从**静默忽略**改为 exit 10（正是 cli-args 头注释描述的缺陷形态在门禁核心的原样残留）；各脚本自有退出码契约不变（用法错 1 或 10 按原契约） |
| B4 | `_lib/destructive-write.mjs` | `writeWithSafety` 改 **temp + rename 原子写**：写入中途被杀不再留半写文件（要么旧文件完好、要么新文件完整）；temp 带 pid+随机段，失败即清理 |
| B14 | `m-gate-check.mjs` | `--report` 落盘失败从「告警即吞」改为 **exit 70**——「闸门必须留机械证据」铁律下，exit 语义不得在证据缺失时照常返回 |
| B9 | `tests/scripts.test.mjs` | 补 M-Form-5 中档（P1 阶）severity 断言（防降档变异） |
| B7 | `.github/workflows/ci.yml` | pack-smoke 进 ubuntu/windows/macos 三平台矩阵（内部含 win32 分支，旧版只在 ubuntu 跑） |
| B8 | `scripts/plugin-surface-check.mjs`、`.github/workflows/publish.yml` | 门禁 CLI pin 0.3.10 → **0.3.16**（新版 CLI 已本地 dlx 实测 exit 0）；publish 的 dispatch 限定默认分支（防未来重签 NPM_TOKEN 后任意分支触发含 token 作业） |
| 回归 | `tests/scripts.test.mjs` | 新增三组用例：A7 参数拒止、B14 落盘失败 exit 70、B4 原子写无 temp 残留 |

**验证**：`node --test` 全量通过（含新增用例）；`repo-hygiene-check` 全绿；`plugin-surface-check` exit 0；`consistency-check` 非 docs 项为零（镜像六个脚本已同步、MD5 核验不一致零）。

**审计 B8 勘误**：原报告称门禁 CLI「未固定版本」——实测 `CLI_SPEC` 早已 pin（且带版本不一致告警），真实问题是 pin 陈旧；本批抬升并保留零下载优先路径。

## 六、第 3 批（同日追加）：结构性（可验证子集）

> 备份：`C:\Users\Zuoyunlai\.dsh\_backup\lunheng-auditfix-batch3-20260921-150412\`。本批只做**可验证子集**；巨石拆分/主控链瘦身/三卡去重见 §七 缓办理由。

| 项 | 文件 | 改动 |
|---|---|---|
| A12 | `references/agents/00-主控-扩展职责.md` | T8 人工裁定双签：M-Form-8 / M-Integrity-2 由 T8 亲裁定（自审自裁）→ 增独立复核（全新上下文 `subagent` 只复核这两项）+ 不一致并列两方结论报主人。同提交抬升该文件词预算 52→53 KB（卫生门第二条合法出路） |
| B3 | `scripts/m-gate-check.mjs` | 新增 `THRESHOLDS` 冻结对象，把散落全文的阈值（M-Form-1 文献下限 / M-Form-3/5/6 严重度档位 / M-Form-8 节扫描与承重墙超载 / M-Exist-1/3 闭环 / M-Form-11 加载率分档 / M-Exist-10 精简段）集中为单一真源——改阈值只改一处 |
| A11 | `.github/workflows/ci.yml` | 新增 `loader-smoke` job：走官方 `dsh-plugin-dev verify`（pnpm pack → 干净 DSH_HOME profile 安装+启动+卸载冒烟），补真实 Loader 层验证 |

**验证**：`node --test` 全量通过；`repo-hygiene-check` 全绿（00-扩展抬限后）；真实项目 M 门抽查 exit 3/2/1/2 无异常（B3 无行为漂移）；`consistency-check` 非 docs 项为零；镜像两文件同步不一致零。

**如实声明（A11 未经本地预跑）**：`loader-smoke` 依赖 `dsh-plugin-dev verify` 在干净 CI（无 Desktop shim）可用，本机受 shim 阻断无法预跑；若 CI 首跑红，先核对 `verify --help` 的 `--dsh/--base` 口径（已写进 job 注释）。

## 七、缓办项（第 3 批剩余，附理由）

- **B2 拆分 m-gate-check / consistency-check 巨石**：需 `run/_mgate-baseline.mjs`（37 真实项目 exit + stdout + report 哈希对账）做等价性闸门，该脚本在仓库外、本会话无法安全跑；B3 阈值集中已为其前置收敛一步。
- **B1 主控链瘦身（00-扩展 53KB）**：牵动多个文档的词预算棘轮与 consistency 门，需一次独立、可回滚的专项迭代。
- **B13 检索三卡去重**：注入防御段是「单段 + 卡片专属措辞（文献卡/数据卡/案例卡）」的模板化重复，机械抽 `_shared` 需引入变量填充步骤、比现有小段重复更脆；价值低、风险高，建议保留现状或改为「三卡各自一行指针 + 共享段免变量」的单独设计。
- **版本号决策**：三批均未升版（package.json/SKILL 仍 18.2.8）；是否并入待发的 18.2.8 合批发布或独立发 18.2.9，留主人决策（同发版前审计 §四）。

## 八、第 4 批（同日追加）：中等与轻微清尾

> 备份：`C:\Users\Zuoyunlai\.dsh\_backup\lunheng-auditfix-batch4-20260921-152133\`。

| 项 | 文件 | 改动 |
|---|---|---|
| B5 | `scripts/token-cost.mjs` | 未显式给 `--price-*` 时响亮告警「默认 DeepSeek 单价、非 DeepSeek 模型将失真」——不再静默当作实价 |
| B9 后半 | `tests/scripts.test.mjs` | 新增「M-Form-5 禁词表逐词注入」契约测试：注入任意一个禁词必须命中，删词即红 |
| B11 | `scripts/apply-diff.mjs` | `stripMeta` 记录被剥离的元注记，进 JSON 契约 `stripped_meta` 字段（防「（依据上述分析）」类正文括号被静默改写后无迹可查） |
| B12 | `references/agents/06-批判`、`05-写作` | 06 卡两个同名「批判报告格式」标题改为「段级清单 / C1-C7」区分；05 卡删除孤儿空编号「11.」 |
| 轻微 | `m-gate-check`、`token-budget`、`count-chars`、`_lib/han.mjs` | U+0001 保护符撞车退化不保护；`'x'.repeat` 改中文填充估 token；U+FFFD 报错附首个坏字符位置；han 口径补「不含扩展 A 区」声明 |
| 轻微 | `checkers/中文AI痕迹-checker.md` | G14-G 删「与 10 篇对比、相似度 >80%」伪精确指标，改可执行的定性判据 |
| 轻微 | `QUICKSTART.md` | Step 1 补「主流程由主控 Phase 0 生成简报，本节是主人草稿」澄清（消除与 Phase 0 路径冲突）；同提交抬升该文件词预算 15→16 KB |
| 轻微 | `.github/publish.yml.draft` | 删除遗留草稿 |

**B10 复核结论（如实）**：`parseCard`/`trustDist`/`ENTRY_ID_RE` 与 `_lib` 各函数**语义不同**（整卡扫描 vs 单卡定位、旧 A/B/C 档 vs 新三档 token、多类型标题锚 vs 单类型），非真重复，强行合并会改语义 → 记为「不适用」，不予合并。
**B6 复核结论（如实）**：Node 最低版本已由 `package.json` engines（`^22.19 || >=24`）覆盖脚本全部隐式要求（top-level await/replaceAll/fetch 均 ≤22.19；`--tree` 需 22.15 已在 token-cost 帮助注明）→ 无需额外声明。

**验证**：`node --test` 全量通过（含逐词注入用例）；`repo-hygiene-check` 全绿；`plugin-surface-check` exit 0；`consistency-check` 非 docs 项为零；镜像十文件同步不一致零。

**四批累计**：审计 12 项重大**全部关闭**；中等项关闭 B3/B4/B5/B7/B8/B9/B11/B12/B14，B1/B2/B13 结构性缓办；轻微项清尾完毕。**版本号仍 18.2.8 未升**，留主人决策。
