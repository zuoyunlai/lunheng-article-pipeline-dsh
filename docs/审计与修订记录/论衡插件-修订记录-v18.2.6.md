# 论衡插件 v18.2.6 修订记录与回滚清单

> **授权依据**：主人直接指令「**依次全部修订**」（2026-09-19）。按本包 `AGENTS.md` §机制文件写保护的**唯一例外**条款——「主人明确下令 = 可写（走安全流程）」——本次机制文件改动**依据主人显式授权**，不属于 agent 自主改机制文件。
> **修订来源**：`E:\HERNESS\论衡插件-全量审计报告-v18.2.5.md`（同日完成的全量审计）§3 阻断项 + §4/§5 P1/P2 + §7 改进方案四批次。
> **改动位置**：全部改动在**真源仓库** `lunheng-article-pipeline-dsh` 完成（教训 #153：实现与测试同居的目录才是真源），验证通过后同步部署镜像。
> **状态**：进行中（本文件随各批次完成实时更新；主控负责最后一批与全文校验）。

---

## 1. 备份与回滚

### 1.1 备份

```
E:\HERNESS\_backup\lunheng-pre-auditfix-20260919-155324\      ← 全量（122 文件 / 1,838,505 B）
E:\HERNESS\_backup\lunheng-pre-auditfix-20260919-155324\_BASELINE-lines.txt   ← 118 个文本文件的行数基线
```

> **与 `AGENTS.md` 流程的一处偏离（如实标注）**：原流程要求备份到「工作区外 `<DSH_HOME>/_backup/`」。
> 本次宿主运行在 `workspace-write` 文件策略下，`C:\Users\<用户>\.dsh\_backup\` **被沙箱拒绝写入**
> （`New-Item` 报 `PermissionDenied`）。故备份改放**工作区内** `E:\HERNESS\_backup\`（与本包
> 「失败产物保留在 `run/` 供人工清理」的既有约定同向）。**如需工作区外副本，请在 host shell 执行第 1.3 节命令。**

### 1.2 涉及范围

`lib/`（4 文件）· `cordis.patch.yml` · `skills/lunheng-article-pipeline/**`（技能体：SKILL.md / AGENTS.md / references / scripts）· 仓库根 `scripts/` · `tests/` · `docs/` · 五语 README · `SECURITY.md` / `CONTRIBUTING.md` / `CHANGELOG.md` · `examples/preset/` · `.github/workflows/`

### 1.3 回滚命令（任选其一）

```powershell
# 方案 A：整包回滚（最彻底；会丢弃本批次全部改动）
$BK='E:\HERNESS\_backup\lunheng-pre-auditfix-20260919-155324'
$REPO='E:\HERNESS\lunheng-article-pipeline-dsh'
Remove-Item "$REPO\*" -Recurse -Force -Exclude '.git'
Copy-Item "$BK\*" $REPO -Recurse -Force

# 方案 B：git 回滚（未提交时最省事；本次改动**未提交**，全部在工作树）
git -C E:\HERNESS\lunheng-article-pipeline-dsh checkout -- .
git -C E:\HERNESS\lunheng-article-pipeline-dsh clean -fd    # 清掉新增文件（_lib/destructive-write.mjs 等）

# 方案 C：单文件回滚
Copy-Item "$BK\lib\guard.js" "$REPO\lib\guard.js" -Force

# 方案 D：把工作区外副本也留一份（host shell 执行，绕过沙箱）
robocopy $BK "C:\Users\<你>\.dsh\_backup\lunheng-pre-auditfix-20260919" /E
```

**回滚验证**：`git -C <repo> status --porcelain -uall` 应为空；`git -C <repo> rev-parse --short HEAD` 应为 `ce6f166`。

---

## 2. 批次 1a：插件契约与写保护（主控亲做，已完成）

| # | 审计编号 | 文件 | 改动 | 验证 |
|---|---|---|---|---|
| 1 | B-5 | `lib/guard.js` | 相对路径解析基准由 `process.cwd()` 改为**会话工作区**（`execution.agent.session.header.cwd`，与 fs 工具同源），并对两个基准取并集判定 | 单测 `B-5 基准错位` PASS（会话工作区 ≠ 进程 cwd 时相对路径被拦） |
| 2 | B-6 | `lib/guard.js` | 新增 `canonicalPath()`：剥 `\\?\` / `\\?\UNC\` 前缀、盘符大写、`realpath`（含"最近存在祖先"回退）、比较忽略大小写 | 12 个路径变体全部 DENY（含小写盘符、长路径前缀、`..` 绕行） |
| 3 | B-6 | `lib/guard.js` | `writtenPaths()` 改**深度遍历**（对象/数组，深度上限 6）+ 从补丁类字段抽 `*** Update File:` / `+++ b/` 形态路径 | 二层嵌套对象、`apply_patch` 补丁文本、unified diff 三例全部 DENY |
| 4 | B-6 | `lib/guard.js` | 新增 `mirrorRoots()`：按官方 rank 表把同名技能的其它落点（100/200/400/500）动态纳入受保护根 | 单测 `B-6 后半` PASS（`.dsh/skills`、`.agents/skills`、`$DSH_HOME/skills` 均被拦） |
| 5 | B-7 | `lib/index.js` | 新增 `export const Config`（**standard-schema 形态，零宿主依赖**）+ `resolveConfig()`；`apply(ctx, config)` 真正消费配置 | `dsh-plugin-dev check` 的 `redline-no-hardcoded-tunables` 由 **skip → 检查**；非法配置经 Cordis 抛 `invalid config: … (at quiet)` |
| 6 | B-3 | `lib/index.js` | 入口不再丢弃 loader 传入的 config；非法配置**加载期响亮失败**（不静默回落默认值） | `verify-config-profile.mjs` 5/5 PASS（合法/非法/未知键三条路径） |
| 7 | B-6 | `lib/index.js` | 受保护根：`packageRoot/scripts` **仅当存在时**登记（npm 装包后该目录不随包 → 旧版是死路径） | 代码级；`_lib` 无该目录时不登记 |
| 8 | P2-13 | `lib/index.js` | `source` 由自贴 `'bundled'` 改为宿主实际档位 `'runtime'`（`RUNTIME_RANK = 250`；`bundled` 在官方 rank 表是 600） | 单测 `source 字段与宿主实际档位一致` PASS |
| 9 | P2-17 | `lib/index.js` | 技能名改由 frontmatter `name` 派生（缺省回落包内字面量），不一致时**响亮告警** | 注册名/`resourceBase` 断言 PASS |
| 10 | P2-19 | `cordis.patch.yml` | 三段分档工具行改「**显式开启才挂载**」：不设任何 `LUNHENG_*` 默认**不装载**；设任一档 `*_PROVIDER/*_MODEL` 或 `TIERING=on` 即装载；`off` 优先级最高 | 4 种 env 组合实测（见 §4 验证证据） |
| 11 | 新发现 | `cordis.patch.yml` | `!!js` 计数披露由「3 处」更正为 **6 处**（漏算 3 处 `disabled:`，而它同样由宿主以完整 Node 权限**加载期**求值） | `^\s*[A-Za-z]+:\s*!!js` 计数 = 6 |
| 12 | P1-4 | `lib/commands.js` | `/lunheng-status` 的 `run/<项目>` 基准改取 `invocation.agent.session.header.cwd`，captured `process.cwd()` 降为兜底 | 代码级 + 未取到会话工作区时在输出里如实提示 |
| 13 | P2-12 | `lib/tools.js` | `runScript` 加**超时**与**输出上限**（来自 `Config`），超时/截断如实回传并在 render 中提示 | 真实 `defineTool` 编译 + `render` 单测 PASS |
| 14 | P2-16 | `lib/tools.js` | `lunheng_char_count.sections` 由 JSON **字符串**改为规范 JSON 值（宿主 DSL 原生支持 `json` 节点） | 工具规格编译 PASS |
| 15 | — | `tests/guard-config.test.mjs` | **新增 9 条回归测试**（含"不误伤"与"授权例外"反向断言） | `node --test --test-isolation=none` → **9/9 pass** |

**批次 1a 验证证据**

```
node --check lib/{index,guard,tools,commands}.js            → 全 exit 0
node <probe>\verify-lib-fixes.mjs                          → PASS 33 / FAIL 0
node <probe>\verify-config-profile.mjs                     → PASS 5 / FAIL 0
node --test --test-isolation=none tests/guard-config.test.mjs → tests 9 / pass 9 / fail 0
node <probe>\check-patch.mjs                               → 4 insert 块 / 3 工具行 / 4 种 env 组合行为符合设计
```

---

## 3. 批次 1b–4：脚本层 / 内容层 / 工程层（并行子线，进行中）

> 本节由主控在各子线交付后填入：每条给出「审计编号 → 文件 → 改动 → 实测证据」。

<!-- S1 脚本 P0-A：md2html 同文件守卫 / build-evidence-bundle fail-open / apply-diff 参数与原地覆盖 / _lib/destructive-write.mjs -->

### 3.1 脚本线 A：P0 数据丢失与 fail-open（已完成）

| # | 审计编号 | 文件 | 改动 | 实测证据（子线报告，主控复核见 §6） |
|---|---|---|---|---|
| 16 | B-1（**数据丢失**） | `scripts/md2html.mjs:44-52,238-241` | 同文件守卫由**字符串相等**改为 `_lib/destructive-write.mjs` 的 `assertNotSameFile()`（`resolve` + `realpathSync` + win32 去大小写 + 去 `\\?\`）；写盘改走 `writeWithSafety()`（目标已存在则先 `.bak`） | 旧版 `md2html a.md ./a.md` → exit 0 且 `a.md` 首行变 `<!DOCTYPE html>`；新版 6 个变体（`a.md a.md` / `./a.md` / `A.MD` / `.\A.Md` / `final/../final/定稿.md` / 绝对=相对）**全部 exit 10 且 SHA256 不变**；正常导出 exit 0 |
| 17 | B-2（fail-open） | `scripts/build-evidence-bundle.mjs:77-91,141-208` | 加**项目形态校验**（`01-任务简报.md`/`final/`/`drafts/` 之一，否则 exit 10，且**排在任何 mkdir 之前**）；`copied===0 && missing>0` → 列出前 5 个缺失源并 exit 10 | 空目录 → exit 10 且**目录内条目数 0**（旧版 exit 0 并在其中建 `audits/`、`final/`、`审计视图-v0.md`）；仓库根路径 → exit 10 无污染 |
| 18 | B-4（假成功+原地覆盖） | `scripts/apply-diff.mjs:42-70,128,162-176,230` | 严格参数解析（未知/缺值/多余位置一律 10）；「清单解析出 0 条」优先判 → exit 1 + `ok:false` + 明确 hint 且**不写盘**；同文件写盘必须显式 `--in-place` 否则 10；`text.replace(locateStr, () => …)` 改函数式 | 空清单 → exit 1（旧版 exit 0 + `ok:true`）；`--out` 缺值 → 10；`--dry-rnu` → 10；`--in-place` → 0 + 生成 `.bak`，且含字面量 `$$P<0.05$$`（旧版被展开成 `$P<0.05$`）；JSON 契约字段原名保留 |
| 19 | — | `scripts/_lib/destructive-write.mjs`（**新建** 96 行） | 零依赖 ESM：`pathKey/realPath/sameFile/assertNotSameFile/backupFile/writeWithSafety` | 被 `md2html` 与 `apply-diff` 共用（不再各写一份守卫） |
| 20 | P1-2（同族） | `scripts/md2html.mjs:196-204` | 缺图**计入 warnings** → `--strict` 不再对「缺图」静默放行 | 与 M-Form-9「缺图 → P0/P1」口径对齐 |
| 21 | P2（BOM） | `scripts/md2html.mjs:59-65`、`scripts/build-evidence-bundle.mjs:35-49` | 读取后剥 BOM + 可见提示（不改退出码） | pwsh `Set-Content -Encoding UTF8` 默认带 BOM，非罕见输入 |
| 22 | 连带 | `tests/scripts.test.mjs`（4 处夹具） | 因 #17 的项目形态校验，4 条既有用例的夹具需补「可复制源」（已转测试所有者；**禁止**通过放松校验来解决） | 见 §5 如实记录 |

**子线如实声明的残余**：① `apply-diff` 在「清单非空但全部跳过」时仍写出与输入逐字相同的 out 文件（exit 1 + `ok:false`，**非静默**，向后兼容保留）；② 软链接与 8.3 短名绕过**无法在本机实测**（建软链需管理员权限、该卷不生成 8.3 短名），`realpath` 归一已在大小写/相对路径/`..`/绝对路径四类实测通过。

<!-- S2 脚本 P0-B：m-gate-check NONSTD_RE / --fig-dir / catch 留痕 / count-chars 口径 / trust.mjs / final-check degraded / token-cost -->

### 3.2 脚本线 B：判定层与假 P0（已完成，主控逐项复核）

**文件**：`m-gate-check.mjs` / `count-chars.mjs` / `final-check.mjs` / `token-cost.mjs` / `normalize-trust-level.mjs` / `_lib/{trust,han}.mjs` + 新增 `_lib/sections.mjs`（**新增**）。

| # | 审计编号 | 改动 | 主控复核 |
|---|---|---|---|
| 23 | B-3 | `NONSTD_RE` 收紧为「1-4 字母或 1-3 汉字前缀 + **≥2 位**序号」；`nonStd` 严重度由 P0 降 P2 | ✅ 亲验：`[COVID-19]/[SARS-CoV-2]/[GPT-4]/[AI-2]/[B2B-2]` 全 exempt、`[XX-12]` 仍抓 |
| 24 | P0-2 | 头注释项数改为与实现一致（`本脚本机检 22 项…总 23 = 机检 22 + 人工 1`） | ✅ 15 项目 `total` 恒 22 |
| 25 | P1-1 | `--fig-dir` 不存在 → exit 10（`requireExistingDir`）；`--fig-dir --report x` 视为缺值 | ✅ 亲验 exit 10 |
| 26 | P1-7 | 九处 `catch {}` 全部留痕；**并查清根因**：`甲醛白菜事件/01-任务简报.md:34` 写作「（主控拆解，**4 个子问题**）」——**数量在标签之前**，旧正则只认「子问题 A/B/C」→ 恒报「研究问题段缺失」= **真解析缺陷**（主控审计时只判为「文案误导」，子线挖得更深）。现三层解析（标签 ∪ 声明数量 ∪ 段内顶层条目）+ 留痕 `subclaimsSource` | ✅ 亲验：该项目现报「4 子问题（口径：声明数量…）」；`test-paper-01`（确无该段）改报「未见「## 研究问题…」段（模板规定必填段）」 |
| 27 | P1-4 | `_lib/trust.mjs` 去 `\b`（与 CJK 相邻失配）+ `pickTrustToken` 只取信任级别行那一档 | ✅ 子线 21 组对照；**真实项目正面副作用**：`platform-gig-social-insurance` 的 `信任级别：**已发布（…）**` 旧判假 P2 并级联假 P0 → `p0 4→3` |
| 28 | P1-9 | `final-check` 可选步骤失败 → `⚠️` + 计入退出码（有 notes 但 exitCode=0 → 改 3「需复核」）；`degraded` 透传并在 recommendation 里明示「hanChars 不可作为权威值」 | ✅ 子线四场景（degraded→3 / 可选失败→3 / 硬错→3 / 干净→0）；**并答复**「中止是既有设计、非其改动引入」（证据：`final-check` 自身报告仍落盘，缺的是 `m-gate-check --report` 产物，而该进程在沙箱里起不来） |
| 29 | P1-5 | `token-cost` 全部会话未命中 → **exit 10** + 指定文案（不用 4，4 已登记为 model-routing 语义） | ✅ 亲验 exit 10 |
| 30 | P2 | `count-chars` 与 M 门「正文区起点」抽到 `_lib/sections.mjs` 共用 | ✅ 亲验：`##  参考文献`（两空格）真实稿 **6280 = 6280**（旧逻辑 6707） |
| 31 | P2 | BOM：`count-chars` 加 U+FFFD 体检 → GBK **exit 10**（旧行为是 `hanChars:0 + degraded + exit 0`，原因还指向「摘要缺失」） | ✅ 亲验 exit 10 + 明确文案 |
| 32 | P2 | `normalize-trust-level` 幂等（对 `信任级别：**已发布**` 不再重复插入）+ 备份位置注释改为如实 | 子线两次运行「1 条已规范化 → 无需改动」 |
| 33 | P2 | `_lib/han.mjs` 去掉有状态 `g` 正则（`test()` 连续三次由 `true,true,false` → `true,true,true`）+ 清死导入 | ✅ 子线实测 |
| 34 | P2 | `tableCells` 不再吞「行尾无 `|`」表格行的末列 | 子线反事实：旧脚本把「关闭状态」判成空串并误报 |
| 35 | **追加** | M-Exist-5 行身份**按检查编号归一**（主控派的跨改动回归收口） | ✅ 亲验 `guannian-yu-linian` 回到 `exit=3/p0=0/p1=0`；子线另给两组反事实（整行删除仍报、编号改成 M-Exist-9 仍报缺 M-Exist-3） |
| 36 | **追加** | M-Exist-6 采**口径 A**：简报 ENOENT（Phase 4.5 前的合法状态）只留痕不影响 pass；「存在但读不动」才推 soft（判据：审稿报告自述优先，硬判定不依赖简报存在） | ✅ 该用例由红转绿 |
| 37 | **追加** | M-Form-4 白名单不再贪婪吞前缀（含角色/流程禁止词时不剥离） | 子线：审计反例「本报告由主控提交给评审机构」现命中 P0；**15 个真实项目判定变化数 = 0**（无新假 P0） |
| 38 | 如实未修 | ① `需找数据点` 不认「数据条目：≥N 条」写法（属**改检查内容**，未越界，detail 如实显示 0 条）；② 三处布局探针 `catch` 与 `token-cost` 的 per-file `catch` 保留；③ `final-check` 端到端只能靠 CI/host shell 复核（沙箱禁子进程管道） | — |

### 3.3 内容线：数字合约与机械门（已完成，主控抽验）

**文件**：`SKILL.md` / `AGENTS.md` / `QUICKSTART.md` / 技能 `README.md` / `references/**`（21 文件）/ `consistency-check.mjs`。

| # | 审计编号 | 改动 | 主控复核 |
|---|---|---|---|
| 39 | C-1 | **M 门 6 种口径 → 唯一真源**：`M-Gate-Algorithm.md:23` 落地可机检的真源行（总 23 = 机械 22 + 人工 1）；`deliverables.md`（13 项）、`08-终检`（20 项 / M-Exist 1-7）、`M-Gate-Algorithm:1051`、技能 `README.md:215`、模板与对照表等全部清仓 | ✅ 亲验 `deliverables.md` 现为指针式表述；门报 **M 门项数不自洽 = 0 条** |
| 40 | C-2 | **M-Exist-3 名实对齐清仓 + 消除假绿行**：模板两行机检表改为与脚本 `gate:` 逐字一致的「引用闭环」+「信任级别完整性（M-Form-6）」；同文件另 3 处与下游 15+ 处同步 | ✅ 亲验模板行名 ↔ 脚本一致；连带回归（15/15 假 P1）已由 M-Exist-5 归一收口 |
| 41 | C-3 | **`M-Gate-Report` schema 重建**（appendix 整篇重写）：22 项 gate 标签逐字表 + 退出码 `0/1/2/3/10/70` + `[报告后激活]` 四项 + 四个 T8 字段与必填条件 | ✅ 亲验：含 `script_exit_raw` / `_t8_llm_review` / 退出码 70；M-Form/M-Exist/M-Integrity 标签齐 |
| 42 | C-4 | 白名单 11 → 12：`00-主控-coordinator.md` 与 `QUICKSTART.md` 改**指针**（消除三处自相矛盾） | ✅ 与 `SKILL.md:43` 一致 |
| 43 | C-5 | `字数判定表.md` 的错误归属改为 **G8 字数核验**，并写明真 M-Form-5 = 过程语言残留 | ✅ |
| 44 | C-6 | **G14 触发时点统一**（早闸 3.6 与 T6 同批 / 终闸 4.5 与 T9 并行）——同步 11 份文档；**`gates/` 与 `checkers/` 两个无归属文件由主控补修** | ✅ 亲验全库无残留（见 §6.7） |
| 45 | 任务 7 | `AGENTS.md` 四条实质错误（补 Phase 3.6 G14、补退码 `70`、删 Phase 4「与 T6 并行」、`node -c`→`node --check`）+ `SKILL.md` 陈旧数字改定性 | ✅ |
| 46 | 任务 8 | `consistency-check` 补断言：⑥b「主语 + 限定词两问」+ ⑥b 的 **6 反例/6 正例**注释、⑩b 改结构派生、㉒ 锚点断言（目标从 `SKILL.md` 派生）、`walk()` 跳 `.git`/`node_modules`、修重复上报 | ✅ 主控亲验注入样本 c（`**22 项**：M-Form 1-11 + M-Exist 1-7…`）**确被抓**；正确仓库上该规则 **0 条** |
| 47 | 词预算 | 常驻集 `SKILL.md + AGENTS.md = 53222 / 53248`（**净增长 −1**）；21 个登记文件全部在上限内 | ✅ 主控复核；另发现 `checkers/中文AI痕迹-checker.md` 越过 12 KB 登记线 → 由主控补登（见 §4） |

<!-- S4 工程层：SECURITY.md / 五语 README / docs / repo-hygiene-check / publish 作业拆分 / CI 矩阵 / 新测试 -->

---

## 4. 批次 5：版本、文档收口与全量回归（已完成）

| # | 动作 | 结果 |
|---|---|---|
| 48 | `package.json#files` 逐文件列出（排除维护者文档 `docs/token-optimization-plan.md`）+ 新增 `test:no-isolation` | ✅ |
| 49 | `examples/preset/preset.yml` 补「默认不挂载」 | ✅ |
| 50 | **补写缺失的 `## 18.2.4` 段**（该版确已发布到 npm：`gitHead 4e25d13` + provenance；全仓引用 37 处却无自己的段） | ✅ 段序 18.2.6 → 18.2.5 → 18.2.4 → 18.2.3，无重复 |
| 51 | **新增 `## 18.2.6` 段**：6 个 P0/P1 的「旧版会怎样 → 现行为」对照、Config 与 pnpm 解析实证、`!!js` 6 处更正、分档 opt-in、M-Exist-3 假绿行与连带回归、测试可跑性、**未采纳 8 项 + 工作区卫生**、验证证据 | ✅ |
| 52 | **`DOC_BUDGET` 补登** `references/checkers/中文AI痕迹-checker.md`（本轮 11.5 → 12.0 KB 越过 12 KB 登记线）→ 上限 13 KB + 理由 | ✅ 登记 22 项，全库 ≥12 KB 的 `.md` **无未登记、无超限** |
| 53 | **版本 bump `18.2.5 → 18.2.6`**：只改版本点位、不动历史注解（58 文件 / 64 处）+ 门抓出的 2 处漏点（`QUICKSTART.md` 标题内嵌版本、`docs/introduction.md` 安装 pin）+ **修掉 bump 脚本自身造成的不一致**（6 个文件里 `git tag v18.2.6 && git push origin v18.2.515` 的 push 端未替换） | ✅ |
| 54 | **镜像同步** `.dsh/skills/lunheng-article-pipeline`（robocopy `/MIR`） | ✅ 88/88 文件**逐字节一致**、无缺失、无多余、无污染（`package.json`/`cordis.patch.yml`/`docs`/`examples`/`lib`/`.git`/`*.tgz` 均无） |
| 55 | **`09-审稿-peer-reviewer.md:99` 陈旧路径修复**（`outputs/先行者清单.md` → `literature/先行者清单.md`）—— 由本轮新增的 `scripts/link-check.mjs` 抓出，**审计报告与内容层子审计都漏了这类**（前者只查链接完整性、后者把该条归为「运行期产物路径」而未追真源） | ✅ 全库无 `outputs/先行者` 残留；文件 14297/14336（余 39 B，未超限） |
| 56 | **给旧化石副本加「非真源」标记**（审计 P2-4 / §7.4 第 27 项） | ✅ `E:\HERNESS\lunheng-article-pipeline\_非真源-历史副本-READ-ME-FIRST.md` |

**版本点数**：`package.json` / 五语 README / `docs/introduction.md` / `docs/troubleshooting.md` / **`SECURITY.md`（此前停在 v18.2.4，落后一版）** / `SKILL.md` / `cordis.patch.yml` 全部 = **18.2.6**；`CHANGELOG.md` 与 `ci.yml` 保留 `18.2.5`（历史记录，正确）。

---

## 6.7 终局验证（一条命令 6 步，全部实跑）

```
① 语法检查（lib + 技能脚本 + 仓库脚本 + 测试）        37 文件 → 语法错误 0            → OK
② 文档一致性门（含镜像同步）                         「66 个 .md + cordis.patch.yml/examples/.dsh 同步，0 处漂移」→ OK
③ 契约线（真实宿主模块）                             verify-lib 33/33 · config 5/5 · patch 4 组合 → OK
④ 测试套件（受限会话降级跑法，含 S4 新增 docs-facts） 108 tests / 105 pass / 0 fail / 3 skipped / exit 0 → OK
⑤ 15 个项目 M 门回放                                 total 恒 22（total≠22 的 0 个）   → OK
⑥ 官方静态门 dsh-plugin-dev check                    9 pass / 0 fail / 1 warn / 4 skip → OK
                                                 ================ PASS 6 / FAIL 0 ================
```

**关键对照（修订前后，同一条命令）**：

| 项 | 修订前 | 修订后 |
|---|---|---|
| `consistency-check` | exit 0 但**门自称覆盖的四处漏点**（SECURITY.md/troubleshooting 版本头停在旧版、脚本计数 11-vs-12 漂移、M 门 6 种口径并存） | **exit 0 且非镜像漂移 = 0**（真漂移已清 + 新增 4 条断言防复发） |
| 测试套件（受限会话） | `node --test` **跑不起来**；降级跑法 102 / **28** / 74 | **108 / 105 / 0 / 3 skip**（3 条为探测式跳过，CI 强度不变） |
| 15 项目 M 门 | `guannian` exit 3、`甲醛白菜事件` exit 2 | **逐项一致**（无回归）；`total` 恒 22 |
| `md2html` 同文件 | `a.md ./a.md` → exit 0 且**源文件被覆盖** | 6 变体全 exit 10 且源文件 SHA256 不变 |
| 写保护 | 相对路径/小写盘符/长路径前缀/嵌套对象**全部可绕过** | 12 变体全 DENY，且保护范围含镜像落点 |
| 插件配置 | `apply(ctx)` 丢弃 `config:`（静默失效） | `Config` + 加载期校验（非法即报 `invalid config`） |

---

## 6. 主控独立复核证据（不只采信子线自述）

### 6.1 lib / 契约线（我亲做，每轮回归）
```
node --check lib/{index,guard,tools,commands}.js        → 全 exit 0
verify-lib-fixes.mjs                                    → PASS 33 / FAIL 0
verify-config-profile.mjs                               → PASS 5 / FAIL 0（Cordis 真加载：合法 config 生效、非法 config 报 invalid config）
check-patch.mjs                                         → 4 insert 块 / 3 工具行正确 / 4 种 env 组合行为符合设计
node --test --test-isolation=none tests/guard-config.test.mjs → tests 9 / pass 9 / fail 0
```

### 6.2 脚本线 A（我亲手复现，每条都以「旧版会怎样」作对照）
| 缺陷 | 我的复现命令要点 | 结果 |
|---|---|---|
| B-1 md2html | `a.md a.md` / `./a.md` / `A.MD` / `final/../final/a.md` | 全部 **exit 10 且源文件 SHA256 不变** |
| B-2 build-evidence-bundle | 空目录 / 仓库根 / 真项目 | **10 / 10 / 0**；空目录**目录内条目 = 0**（旧版会建 `audits/`、`final/`） |
| B-4 apply-diff | 空清单 / `--out` 缺值 / `--dry-rnu` / 无 `--in-place` / `--in-place` / `--out` | **1 / 10 / 10 / 10 / 0+.bak / 0**；`$$P<0.05$$` 保持字面量 |
| 旗标收紧（追加项） | `build-evidence-bundle --sumary` | 旧版 exit 0（当真跑了全量）→ 新版 **exit 10 + 用法** |

### 6.3 脚本线 B（我亲手复现）
- `NONSTD_RE` 现为 `/\[(?!D-基-)([A-Z]{1,4}|[\u4e00-\u9fff]{1,3})-(\d{2,})\]/g`：`[COVID-19]/[SARS-CoV-2]/[GPT-4]/[AI-2]/[B2B-2]` **全部 exempt**，`[XX-12]` 仍被抓（B-3 修复）。
- `--fig-dir <不存在>` → **exit 10**（旧版静默丢弃并改用猜测目录，结论反而更宽松）。
- 15 个真实项目 `total` 恒为 **22**（契约未变）。

### 6.4 我抓到并派单收口的两条**跨改动问题**
1. **跨改动回归（已收口）**：内容线把闸门记录模板的「信任级别一致性（M-Exist-3）」改名为「引用闭环（M-Exist-3：…）」（**审计 C-2 要求的正确修复**），但 M-Exist-5 的规则是「模板为真源，逐项都要有行」→ **15/15 真实项目全部新增一条 P1**，其中 `guannian-yu-linian` 由 `exit 3` 退化成 `exit 1`（exit 1 会触发 T5 修订轮 = 假 P1 阻断交付）。→ 已要求把「行身份」判据改为**按该行引用的检查编号归一**（`引用闭环（M-Exist-3：…）` 与 `信任级别一致性（M-Exist-3）` 归为同一行）。**复核结果：已收口**——`guannian-yu-linian` 回放到 `exit=3 / p0=0 / p1=0 / p2=4`，与审计基线一致。
2. **新机检规则误报（已派单）**：内容线新增的「M 门项数自洽」规则抓出 26 处，我逐行核对发现**约 8 处是误报**（在**修规则前**若照单全改会把正确文字改错）：`M-Gate-Algorithm.md:74`（「22 项中 10 项需人工修正」）、`:125`（「M-Form …（11 项）」= 分项小计，本来就对）、`:661`（M-Exist 10 项，同理）、`audit-checklist-quickref.md:14`（G 清单 15 项）、`:102/:103`（分项小计）、`00-主控-扩展职责.md:488`（「其中 4 项 P0」）、`07-审计-auditor.md:53`（同 `:74`）。→ 已要求把规则改成**带主语与限定词的两问**，并把上述 8 条写成注释里的反例清单。**真漂移**（已确认，待改）：`deliverables.md:135-137`（8/3/13）、`08-终检:36`（20）、`appendix`（M-Form 区间 7）、`DSH-集成方案.md:66`（机械化 23 应为 22）、`docs/introduction.md:68,91`、`docs/troubleshooting.md:170`（20/19）。

### 6.5 测试套件可跑性（本次修订的意外高价值收益）
| 形态 | 结果 |
|---|---|
| 修订前 · `node --test`（标准隔离） | **跑不起来**：runner 自身 spawn 子进程 EPERM |
| 修订前 · `--test-isolation=none` | tests 102 / pass **28** / fail 74（74 条几乎全是 `_fixtures.mjs` 管道禁令的同一根因） |
| 修订后 · `--test-isolation=none` | **tests 104 / pass 101 / fail 0 / skipped 3，exit 0** |
| 3 条 skip | 全部是**探测式**条件跳过（探测到「宿主禁止子进程开命名管道」或「`npm pack` 不可用」才跳过），skip 理由写明了「本环境为何不行 + 在哪种环境下会真跑 + 不得用 LLM 断言替代机检」；**无沙箱环境下强度与之前完全一致** |
| 踩到的一个坑（已排除） | 用 `\| Select-String` 管道跑会看到 exit 1（PowerShell 管道假象）；**重定向到文件后实测 exit 0** —— 记录在此以免后续复核误判 |

### 6.6 15 个项目 M 门回放基线（供最终 diff）
`E:\HERNESS\.tmp-review\mgate-after.json`（15 项目，**total 恒 22**）。关键对照：`guannian-yu-linian` exit=3 p0=0 p1=0 p2=4、`甲醛白菜事件` exit=2 p0=5 p1=2 p2=1 —— **与修订前审计基线逐项一致**（无回归）；`bigdata-ai-sociology` exit=0（全门通过样本）。

---

## 7. 如实记录：未做与打折项（收尾时补全）

- **`m-gate-check.mjs` 的完整拆分（审计 §7.4 第 21 项）打折为「先去重、后拆分」**：该文件 2,005 行 / 22 个门，拆分必须同步改两个把它当**单个文本 blob** 解析的门（`consistency-check.mjs` 的 `GATE_DERIVED`、`repo-hygiene-check.mjs` 的派生与脚本计数），否则门会**静默失效**。本次先做零风险部分（合并重复的 `dirname(dirname(draftPath))` 内联拷贝到既有模块级常量），完整拆分留待独立批次并附迁移方案。
- **受限会话下 `lib/tools.js` 的两条可用性缺口（如实保留）**：原生工具在**宿主进程**内 spawn，故不受沙箱管道禁令影响；但在「测试进程/受限 shell 内」调用工具会 EPERM —— 已改为**可操作报错**（点名改用 `pwsh` 调同一脚本 + 禁止用 LLM 断言替代机检），并把这条降级路径写进测试的探测式跳过。
- **`apply-diff` 的既有边界（子线如实声明，未处理）**：当「现况/修改」仅在句末标点处差异时，最小差异法抽取退化，输出行会出现重复片段；已用对照副本确认**与本次改动无关**（新旧输出逐字相同），属 `extractDelta`/`locateAndReplace` 的独立课题。
- **软链接与 Windows 8.3 短名绕过未能在本机实测**（建软链需管理员权限、该卷不生成 8.3 短名）；`realpath` 归一已在大小写 / 相对路径 / `..` / 绝对路径四类实测通过。
- **`plugin-surface-check` 仍无法在本机跑通**（需联网取 CLI，且 `npm`/`spawnSync` 被沙箱拦）；该门的本地复现命令与「已实测不可跑」的事实写入文档，不硬改以绕过环境。

