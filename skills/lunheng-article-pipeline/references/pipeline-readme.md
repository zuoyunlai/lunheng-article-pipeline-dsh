> 版本：v2.3.7-dsh.5（DSH 适配版，对应正典 v2.3.7，自动同步 2026-08-22）

# 论衡（lunheng-article-pipeline）— 通用深度长文多 Agent 流水线 运行手册（v2.2.14）

> **定位（v2.2.4）**：论衡是**通用深度长文引擎**，不只用于公众号，而是可产出**学术论文、商业评论、行业分析、公众号深度长文**等各类深度长文。学术论文用 `[Lxx]/[Dxx]` 编号引用；公众号/商业评论用内联（机构，年份）引用（M 门 v2.2.4 新增内联引用分支）。

> 使用方式：主控 Agent（论衡）按本文档派发任务。角色定义见 `agents/`，模板见 `templates/`。

## 📖 核心概念词汇表（单一真源，优先阅读）

**开始使用前，强烈建议先阅读**：[`glossary.md`](glossary.md)

词汇表集中定义了所有核心概念，避免在多个文档中重复查找：
- 8 张角色卡（T0-T7）+ T8 终检职责（v2.3.0 重构：T6 案例检索 → T3，T6 = 批判伙伴，T7 = 审计员，T8 = 终检主控亲完成）
- 三层防御体系（M 门/F 模式/G 清单）
- 数据信任级别（3 档）
- 关键协议（Phase 0/0 条空卡/并行独立运行/修订回环/完成验证铁律）
- 工具能力边界（DSH：工具集由 Agent 预设决定，见 `SKILL.md`「DSH 适配说明」）
- 版本号管理（5 层真源）
- 教训沉淀体系（#1-#115，v2.3.0 加 #116 README 内部冲突 + #117 修订回环 ≤2 轮用满）

**为什么要先读词汇表**：
- 减少文档冗余，避免同一概念在多处重复定义
- 建立统一的术语体系，降低理解成本
- 快速索引关键概念，提升查询效率

---

> **⚠️ v2.1.7 Phase 0 强同意关卡（ClawHub scanner F5 修正，DSH 下保留）**：本流水线下面任何一个阶段调用以下工具，**会**发数据到**外部服务商**——主控必须告诉主人明示同意，否则不可调用：
>
> | 工具 | 外发服务商 | 发送内容 |
> |------|---------|---------|
> | `web_search` / `read_page` | DSH 配置的 web provider（DSH 无独立 Tavily 工具） | 查询关键词 / URL |
> | 封面/插图 | DSH 无内置文生图：默认 SVG 矢量风（本地，不外发）或主人投喂；如需文生图另配图像 MCP（如 MiniMax image-01，外发） | 主题 + prompt |
> | 主控推理调用 | 当前模型 provider（deepseek/MiniMax/Anthropic） | 文献/数据/案例/草稿/大纲/全文 |
>
> **项目名/主题/论文纲要可能含未公开信息**——主控 Phase 0 必须问主人：
> 1. 同意全部外发？（默认）
> 2. 脱敏措辞 + SVG 封面 + 本地模型推理（如 Ollama 等本地 provider）？
> 3. 只同意一部分（如文献检索同意、封面文生图拒绝）？
>
> 主人选定后写入 `01-任务简报.md` 作为「外发同意项」记录。**未同意前不可跳到 Phase 1**。
> v2.0（2026-08-11）：新增 Phase 2.5 大纲确认、status.md 状态机、修订说明、引用分级核验、数据时效评级、防选题漂移 G0 检查、AI 去味禁做清单、证据包交付、期刊模板。
> v2.1（2026-08-11）：新增配图节点（Phase 4.5）。v2.2（2026-08-13）：新增案例卡 + T3 案例检索员 + 三角验证 + G2.5 + 时间锚点 + 中文新闻源 + 强相关性铁律（教训 #34）。v2.3（2026-08-13）：调度预检 + 8 分钟硬卡 + 写手视角一致性/数据无源/图注精度 + 审计 G0.5/G6 + 融合协议 + 反哺报告 + 论据类型自标（教训 #35）。v2.4（2026-08-13）：**原创性保证**——T1 先行者检索 + T3 差异点声明 + 审计 G7 原创性审计（防「重复/改写已公开文章」）。v2.0.2（2026-08-14）：**安全收紧**——回应 ClawHub SQP-2 finding：反哺报告不自动 commit 角色卡 + Phase 0 显式确认 <项目名> 与文件清单 + 工具集收窄到 13 项（移除 exec/browser/cron/凭据访问路径）。v2.0.5（2026-08-14）：封面恢复 image_generate 工具（默认 OpenAI gpt-image-2，考虑普适性），回应教训 #38 over-correction（工具白名单变 15 项）。v2.0.6（2026-08-14）：T3 案例检索员干跑验证完成 + Skill 包补 设计文档.md + 回应 ClawHub Finding 2（drafts/初稿-v1.md 写入加 v2/v3 覆盖声明）+ Agent README 版本变更行重复清理 + 文档同步漂移修复（MEMORY.md 13→15 工具数 / 1→2 findings / SOUL 误述）。v2.0.8（2026-08-14）：image_generate fallback 链补充 minimax/minimax-image-01 作为最终 fallback（论衡 agent imageGenerationModel.primary 仍保持 OpenAI gpt-image-2 普适默认，fallback 顺序 OpenAI → Google → minimax → SVG）。v2.1.0（2026-08-14）：**激进重构——执行层韧化 + 审计补盲区**。**执行层**：7 张角色卡全部注入「执行韧化协议」——30 秒心跳 + 分阶段 ack（按任务时长 3 档分级）+ 模型健康度预检（1-token ping，30s 无响应降级 fallback）+ 8 分钟硬卡（6 分警告/7 分 partial/8 分 kill）；论衡 agent `model.fallbacks` 扩展为 4 档（minimax-M3 → deepseek-flash → glm-5.2）；主控「介入机制」补 6 步标准动作（拍醒/查 子代理/换模型重派/接受 partial/写介入日志/重试上限 2 次）。**审计补盲区**：T5 新增 **G8 成品度**（8 项必查：过程语言/角色元数据/临时编号/元话语/修订文件/占位符/结构对称/字数偏差）+ **G9 时序合理性**（4 项必查：总耗时/分阶段 ack/心跳记录/文件时间戳）。回应教训 #43（执行层脆弱 + 成品度盲区）。v2.1.1（2026-08-14）：**回应 ClawHub 7 项新 findings**。① 修写手矛盾（95% confidence）：禁做清单 #3「主体声音 ≠ 第一人称经历」替换原「无第一人称」混淆表述；② 限「据行业经验估算」使用条件（92%）：主控洞察融合协议加 5 条限定（三源全失败 + 段落顶标 [行业估算] + 不超 3 处 + T5 G2.5 估算专项 + 禁伪装精确度）；③ 首次 image_generate 需主人同意（91%）：主控/SKILL.md 加「封面生成前必须先询问」机制；④ 分析员触发条件段（89%）：顶部加 allowedCallers/when/exclusions/boundary check 四部分；⑤ ⑥ ⑦ #2/#6/#7（语言/文件写入/硬编码 locale）部分预期内不修 #2/#6/#7 部分预期内不修，README 加语言与定位声明。教训 #44 已写。v2.1.2（2026-08-15）：**补 ClawHub F5/F9 外部传输警告 + 文档同步修复**。① SKILL.md / README.md / 论衡 `references/_shared/failure-modes.md` / references/pipeline-readme.md 加「⚠️ 外部服务与数据流声明」（明示 Tavily / OpenAI gpt-image-2 / Google gemini / minimax-image 等外发项 + 主人拒绝调整方案 + 脱敏/SVG/本地 Ollama 备选）；② 主控卡加 Phase 0 外部服务告知职责；③ 设计文档.md 团队架构补 T3 案例检索员可视化（之前第三个并行框为空）+ 7 角色编号完整化 + G8/G9 + v2.1.0/v2.1.1 增量补全；④ status-template.md + 任务简报-template.md 「T6 终检交付」→「T7 终检交付」编号冲突修复（T6 已是案例检索员）+ 新增 T6 案例检索行（可选，默认 Skipped）；⑤ `references/_shared/failure-modes.md` team 表补 T6 + v2.1.1 安全约定同步；⑥ 论衡 MEMORY.md 版本号 v2.3→v2.1.1 + 角色卡数 6→7 + 执行韧化协议补全；⑦ AGENTS.md 流水线协议加 T6 + Phase 0 外部服务 + 8 分钟硬卡；⑧ 主控 workspace MEMORY.md 订正「ClawHub verdict=BENIGN」误述（实际 Review/10 项 findings）。教训 #45 已写。v2.1.3（2026-08-15）：**执行层可靠性补强 + 审计一致性 + 元数据边界**（教师场域孤岛实战教训 #46）。① 主控「完成验证铁律」——收到完成报告第一动作 ls 验证产物存在+非空+关键 grep，不采信报告（T4 v3/v4 两次「报告完成但未写盘」事故）；② 修订任务目标拆分——>2 个独立目标拆任务或主控 edit；③ 小修订主控直接 edit——定点修复 <5 处不 spawn T4；④ T5 新增 G10 核心模型一致性审计（五方主体三处表述不一致事故）；⑤ 写手铁律 #8 元数据边界——[xx说明]/GAP标注/图位详细说明不进正文；⑥ 终检必查项加统计口径动态更新 + 先行者引用闭环（[先xx] 文末对应）；⑦ 主人在环三节点→四节点（新增 Phase 3.5 主人深度洞察补充窗口）。教训 #46 已写。v2.2.0（2026-08-15~18）：P0-A 机械硬门（M 门 + comm diff）+ P0-B 失败模式清单（F1-F7 替代 G 部分）+ P0-D max 2 revision + P1-C 报告隔离（教训 #64）。v2.2.1（2026-08-18）：P0-C 数据信任级别 + P1-A 双完整性门 T2.5/T5.5（教训 #77）。v2.2.1.1（2026-08-18）：补文献卡 + 先行者清单 template（教训 #78）。v2.2.1.2（2026-08-18）：3 个 M 门算法修复 + 数据卡双格式支持（教训 #85）。v2.2.2（2026-08-18）：P1-B 批判伙伴 T8 + P2-A Disclosure 模式（教训 #86，路线图三发最后一发）。v2.2.3（2026-08-18）：**GLM-5.2 → GLM-5.3 全栈迁移**——GLM-5.2 即将下线 + DSH 配置 ID 错配（实际 chat ID 是 glm-5-2-260617，教训 #89）；论衡角色卡 + _shared 文件 + README + 任务简报 + ClawHub 副本全部替换为 coding-plan/glm-5.3；DSH config 删除 GLM-5.2 实体。教训 #90。v2.2.4（2026-08-19，AI安全隐患实战闭环 + 深度长文定位）：**7 项改进**——① 修订 SOP 固化（先 cp v2 再改 v2，归档中间态，哈希校验，教训：实战 P1-2）；② 修订轮强制独立写手（主控不代执行，防自我说服）；③ M 门内联引用分支（公众号/商业评论内联引用下 M-Exist-1 不再空转，手动回溯 24 条）；④ T4 无主数据自查（不得凭记忆写未经核实数字，实战误写 2020 年 2500 万深伪案）；⑤ T5 修订任务书（打回时产出可执行修订表）；⑥ 补检索文献回填校验（补检索 L 总数 == 文末清单条目数）；⑦ 第 3 轮出口正常化（局限性模式是诚实出口非失败）。**定位升级：深度长文引擎**——学术论文/商业评论/行业分析/公众号深度长文通用。v2.2.5（2026-08-19）：对 v2.2.4 自指质量审计，修复 4×P1（设计文档漏 commit / 补检索回填等式错误 / 内联引用系统化 / 修订轮边界冲突）+ 6×P2。v2.2.6（2026-08-19）：**版本升级自审门**——新增 `_shared/版本升级自审门-v2.2.6.md`（7 门机械化自审，教训 #95），主控卡加引用；背景：v2.2.2 新增 T8 时 SKILL.md 三处漏同步，主人人工发现才补。

v2.1.4（2026-08-15，**测试期 finding，本地修订未发布**）：**文末「引用来源」四节完整性闭环**（教师场域孤岛实战教训 #47）。① README「定稿引用规范」升两节→**四节**（数据来源/案例来源/参考文献/先行者文献，[C-主xx] 主人洞察单独说明）；② 写手卡铁律 #9「文末「引用来源」清单完整性」——双向 diff 自检 + 测算值如实标注；③ 主控卡终检必查项 ⑨「文末四节完整性闭环」——机械化 grep diff 双向校验；④ 审计卡 G4「文末「引用来源」四节完整性」——任意漏引/孤儿条目 P1；⑤ 交付前必走 `grep -oE "[DC]-?[LC]-?[主]?[xx]+" + comm -23/-13` 三件套。教训 #47 已写。

**v2.1.4 修复的具体事故**：教师场域孤岛定稿主控对话环节主人发现文末缺数据/案例两节来源清单（35 条 [Dxx] + 8 条 [Cxx]/[C-主xx]），写手只搬运了 [Lxx] 参考文献一节，主控终检未卡，审计 G4 未覆盖 final 阶段。机制升级确保后续项目四节完整无漏。

## 目录

- [🚫 不适用场景（边界明示 — v2.1.5 新增，v2.1.5.1 修正表述）](#不适用场景边界明示-v215-新增v2151-修正表述)
- [快速开始](#快速开始)
- [调度模型（DSH 版，替代原 OpenClaw 健康度预检）](#调度模型dsh-版替代原-openclaw-健康度预检)
- [流水线全景](#流水线全景)
- [status.md 状态机（主控维护，角色交接时更新）](#statusmd-状态机主控维护角色交接时更新)
- [派发话术（主控复制即用）](#派发话术主控复制即用)
  - [文献检索员（并行①）](#文献检索员并行①)
  - [数据检索员（并行②）](#数据检索员并行②)
  - [案例检索员（v2.1.8 强化：任何量级必 spawn，与 T1∥T2 三方并行互不干涉，教训 #56）](#案例检索员v218-强化任何量级必-spawn与-t1t2-三方并行互不干涉教训-56)
  - [分析员](#分析员)
  - [写手](#写手)
  - [批判伙伴（v2.2.2 新增：写手 v1 后、审计前 spawn，教训 #86）](#批判伙伴v222-新增写手-v1-后审计前-spawn教训-86)
  - [审计员](#审计员)
- [修订回环](#修订回环)
  - [v2.2.0 修订回环 ≤2 轮硬约束（教训 #64 子教训 3）](#v220-修订回环-2-轮硬约束教训-64-子教训-3)
- [M 机械化硬门（v2.2.0 新增，教训 #64 + #65）](#m-机械化硬门v220-新增教训-64-65)
  - [M 门算法规约（v2.2.0 哲学最纯粹版）](#m-门算法规约v220-哲学最纯粹版)
- [F 失败模式防御（v2.2.0 新增，教训 #64）](#f-失败模式防御v220-新增教训-64)
  - [F1-F7 速查](#f1-f7-速查)
- [F8 数据信任失败模式（v2.2.1 新增，教训 #77）](#f8-数据信任失败模式v221-新增教训-77)
- [F9 论证强度不足（v2.2.2 新增，教训 #86）](#f9-论证强度不足v222-新增教训-86)
- [AI 使用披露（Disclosure，v2.2.2 新增，教训 #86）](#ai-使用披露disclosurev222-新增教训-86)
- [交付（Phase 5）](#交付phase-5)
  - [定稿引用规范（主人 2026-08-12 确立，水与麦单修正 + v2.1.4 四节闭环，教训 #47）](#定稿引用规范主人-2026-08-12-确立水与麦单修正-v214-四节闭环教训-47)
  - [PDF 生成规范（主控负责，水与麦单修正）](#pdf-生成规范主控负责水与麦单修正)
  - [修订规范（教训 #11：改稿必先另存版本号）](#修订规范教训-11改稿必先另存版本号)
- [配图生成规范（Phase 4.5）](#配图生成规范phase-45)
- [成本与模型建议](#成本与模型建议)
- [模型配置与更换指南（v2.3）](#模型配置与更换指南v23)
  - [三层模型配置（从外到内）](#三层模型配置从外到内)
  - [更换的自由、途径、权限](#更换的自由途径权限)
  - [能力分层原则（换模型时遵循）](#能力分层原则换模型时遵循)

---

## 🚫 不适用场景（边界明示 — v2.1.5 新增，v2.1.5.1 修正表述）

论衡是「论文/深度文章」**写作流水线**，**擅长主动检索已发布证据 + 整合主人投喂的证据**。

**论衡能主动采集**（T1 文献检索 / T2 数据检索 / T3 案例检索 sub-agent，v2.3.0 改 T6→T3）：
- ✅ 已发布的学术文献（PubMed / CNKI / Web of Science 等数据库）
- ✅ 已发布的统计数据（教育部 / 统计局 / 行业协会等公开数据）
- ✅ 已发布的案例与报道（媒体 / 法院判决 / 行业报告等公开案例）
- ✅ 政府发布的统计 / 报告 / 调查 / 政策文件

**论衡不能主动采集**（需要主人提前准备素材投喂）：
- ❌ **一手原始数据采集**：实验设计 / 调查问卷投放 / 用户访谈 / 田野调查 → 需要主人亲自调研，原始数据投喂为「数据源」
- ❌ **统计分析**（SPSS/R/Python 跑模型）：论衡可以引用统计结果，但**不执行统计计算**。如需跑回归/聚类/因子分析，请主人用专门工具，结论以「数据 + 方法描述 + 结果」形式投喂
- ❌ **图表原始数据采集**：论衡生成的是**数据可视化**（SVG 矢量图，参考 `图表-SVG-template.md`），数据本身需主人提供。如需爬虫/OCR/语音转文字，请主人用专门工具，原始数据投喂后论衡制作图表
- ❌ **原创图片 / 视频生成**：DSH 无内置文生图（原 OpenClaw `image_generate` 不可用）——封面/插图降级为 **SVG 矢量风（程序化，本地）或主人投喂图片**，或另配图像生成 MCP（如 MiniMax `image-01`）；**不能拍摄实物照片 / 录制视频**。如需实物素材，请主人拍摄后投喂文件路径，论衡可在文末引用
- ❌ **代码执行**：原 OpenClaw 环境 `exec` 被 deny；DSH standard 预设含 `pwsh`/`bash`，如需跑代码验证论据，经主人同意后主控/子代理可执行，结果投喂为证据

**判断口诀**：问「这个证据是**已发布**的数据 / 文献 / 案例吗」——是，论衡主动采集；不是（是一手原始数据 / 自己拍的素材 / 自己跑的计算），主人投喂后再用。

## 快速开始

```
1. 主控确认主题（Phase 0）→ 写 01-任务简报.md + 初始化 status.md
2. 主控记录 cases 需求（含 0 条场景的显式声明），写入 status.md（v2.1.8 取消三档决策，教训 #56）
3. 主控确认模型路由（DSH：`settings.yaml` / 分档预设，详见下节）→ 决定该次 T 派发用哪个模型
4. 并行 spawn 文献检索员 + 数据检索员 + 案例检索员（用下面的派发话术）
5. 等子代理完成通知 等两者完成
6. spawn 分析员 → 分析大纲产出 → Phase 2.5 送主人确认 → 通过后 spawn 写手
7. spawn 审计员 → 审计不过 → 写手交修订说明 + 修订稿（≤2 轮）→ 终检交付（含证据包）
```

## 调度模型（DSH 版，替代原 OpenClaw 健康度预检）

> 背景：OpenClaw 用「模型健康度预检 + 脚本级 fallback 链」（教训 #35/#119）；**DSH 无脚本级 fallback 链**，模型由 `settings.yaml` 会话级路由 + 分档预设按角色指派，超时由主控人工介入。以下为 DSH 下的等价机制：

**主控 T 派发前必须明确 2 点**：

1. **模型路由（按角色分模型，可选）**：
   - 会话级模型由 `settings.yaml` 决定（默认所有角色继承）；
   - 按角色分模型装「分档预设」（`examples/preset/`）：`subagent_retrieval`（T1/T2/T3，默认 `deepseek-v4-flash`）/ `subagent_strong`（T4/T5/T6，默认 `deepseek-v4-pro`）/ `subagent_audit`（T7，默认 `deepseek-v4-pro`，可经 `LUNHENG_AUDIT_MODEL` 换 minimax-M3/claude-opus-5 等顶配）；
   - 未装预设则全部继承会话模型，流水线照常运行。

2. **超时介入（DSH 精简版，无硬卡）**（OpenClaw 曾用 8 分钟硬卡 + 自动 kill）：
   - T 派发后长时间无产出 → 主控用 `list_agents` 查看子代理实际状态，判定异常即介入（换档重派 / 主控兜底）；
   - 不允许「静默空等」——超时判断以子代理实际状态为准；
   - 审计子代理（T7）建议顶配模型：若静默无产出（教训 #119），主控换档重派。

3. **主控兜底产物明确标注**（v2.0 机制没标 → 写手不知道这是主控产物不是 T 产物）：
   - 任何 T 阶段主控亲自产出 → 必须在文件头标注「主控兜底 v1.0」+ 注明失败原因
   - 写手收到时能识别这是非 T 产物，调整引用对齐策略

## 流水线全景

```
Phase 0 定题       主人确认主题/期刊模板/篇幅 → 01-任务简报.md + status.md
Phase 1 并行检索   T1 文献检索员(文献卡A/B/C分级+信任级别已发布) ∥ T2 数据检索员(数据卡🟢🟡🔴+冲突并列+信任级别已发布/主人投喂/二手转引，v2.2.1 必填)
                ∥ **T3** 案例检索员(cases/案例卡.md+信任级别已发布/主人投喂/二手转引，**任何量级必 spawn**，含 0 条场景走空卡协议，v2.3.0 重命名原 T6→T3)
[🔒 T2.5 完整性门 v2.2.1 主控 checkpoint]：数据条目数≥任务简报需求数 + 信任级别完整 → 通过才派 **T4**（v2.3.0 改 T3→T4，不通过→触发 T2 重检索或主控补数据；v2.3.2 删主人签字，教训 #136）
Phase 2 分析       **T4** 分析员 → 分析大纲.md（论点-论据映射 + 反方论证 + v2.2 三角验证标注，v2.3.0 改 T3→T4）
Phase 2.5 大纲确认 主人过目大纲 → 确认/修改（人在环，改方向成本最低）；主控把关三角验证（[L]+[D]+[C]）
Phase 3 写作       **T5** 写手 → 初稿-v1.md（禁做清单 + 反方论证 + 字数预算；引用标[Lxx]/数字标[Dxx]/案例标[Cxx]；修订 v2/v3 显式覆盖同路径，v2.3.0 改 T4→T5）
Phase 3.5 洞察补充 主人过目初稿 v1 → 主控问主人洞要补 → T5 写手 v2 融入（人在环，v2.1.3 教训 #46）
[🔒 **T6** 批判伙伴 v2.2.2 主控 spawn，v2.3.0 改 T8→T6，v2.3.0 引入 Phase 3.6 独立节点]：T5 写手 v2（已含主人洞察）后、T7 审计前，从反方攻击论证（C1-C5）；轻量档（≤3000 字）可跳过；产出 analysis/批判报告-vN.md
Phase 4 审计       **T7** 审计员 → 审计报告（G0覆盖度/G1分级核验/G2溯源/G2.5案例核验/G3逻辑/G4格式/G5规范/G11时效告警/G12信任级别一致性 v2.2.1 新增/G13 AI 使用披露 v2.2.2 新增，v2.3.0 改 T5→T7）
Phase 4.2 修订     写手交 修订说明 + 修订稿 → 审计员复核 → 最多 2 轮 → 仍不过升级主控（v2.2.0 ≤2轮硬约束+ Acknowledged Limitations 模式）
Phase 4.5 配图     仅当简报「配图需求」非无：写手已在正文标 [图N] 图位 → 主控 `write` 手写 SVG 生成数据图表（数字与数据卡/[Cxx] 一致，参考 `图表-SVG-template.md`）
[🔒 **T7.5** 完整性门 v2.2.1 主控 checkpoint，v2.3.0 改 T5.5→T7.5]：审计报告最新版 + P0/P1 清单 + M 门全 exit 0 + 论文交付物 vs 操作员报告隔离 → 通过才派 **T8**（v2.3.0 改 T7→T8，不通过→触发 T7 重审或主控补审；v2.3.3 删主人签字，教训 #138）
Phase 5 终检      **T8** 主控终检 → final/定稿.md + 图件/（如有）+ 证据包/(文献+数据+案例+审计) + 交付说明.md + M-Gate-Report-v2.2.4.json → 高频问题反哺报告（不自动 commit 角色卡 — v2.0.2，v2.3.0 改 T7→T8）
```

**🔒 = v2.2.1 主控阶段闸门（教训 #77）**——T2.5 在 T2 → T4 间，T7.5 在 T7 → T8 间（v2.3.0 改 T3→T4/T5.5→T7.5/T7→T8）；不引入新 agent，主控用 `todo_write` + `read` 工具实现；**「不绕过交接直接派发」**。

## status.md 状态机（主控维护，角色交接时更新）

每个任务状态：`Inbox → Assigned → In Progress → Review → Done | Failed`
- 每个角色交接时必须更新 `status.md` 对应行
- 失败要留原因；任一行长时间无进展 → 主控介入（`list_agents` 查看，DSH 精简版无硬卡）
- 模板：`templates/status-template-lite.md`（精简版实战用）/ `templates/status-template.md`（完整版调试用）

## 模板加载策略（v2.2.14 优化，节省 80%）

| 场景 | 加载版本 | 原因 |
|------|---------|------|
| 实战项目（Phase 1-5） | **`*-template-lite.md`** | 精简版只含骨架 + 必填字段 |
| 培训新人 / 调试模板 | **`*-template.md`** | 完整版含字段详解 + 实战案例 |

7 个模板精简版总大小 8.7KB（vs 完整版 42.6KB），**节省 80%**。主控实战派发时优先用精简版。详见 `templates/README-模板拆分方案.md`。

## 设计文档加载策略（v2.2.16 优化，节省 89%）

| 场景 | 加载版本 | 原因 |
|------|---------|------|
| 实战项目（Phase 0-5） | **`设计文档-架构.md`** | 架构篇只含团队架构 + 角色定义 + 工作流程 |
| 培训新人 / 哲学理解 | **`设计文档-哲学.md`** | 哲学篇含关键防坑 + 与单 AI 区别 |

原 32.9KB 设计文档拆分为架构版（3.6KB）+ 哲学版（1.8KB），**实战节省 89%**。主控实战派发时优先用架构版。详见 [`../`references/operations.md``](../`references/operations.md`)。

## 派发话术（主控复制即用）

> **v2.3.0 补全（教训 #118）**：原文件只有 T1/T2 两个派发话术，缺 T3 案例检索员、T4 分析员、T5 写手、T6 批判伙伴、T7 审计员；T8 终检 = 主控亲完成不需派发话术。SKILL.md §87 「T1/T2/T3/T4/T5/T6/T7/T8 八个角色完整派发模板见 references/pipeline-readme.md#派发话术」原是虚假引用，现补齐。
>
> **使用说明**：完整角色职责/铁律在 `references/agents/0X-*.md`（8 张卡），下面只列「主控复制即用」的最小派发话术。优先级：完整角色卡 > 派发话术。

### 文献检索员（并行①，T1）

```
你是「文献检索员」。任务编号 T1。
**你的完整职责/铁律见 references/agents/01-文献检索-literature-scout.md（v2.2.12 先读再干活）**
项目目录：run/<项目名>/
任务简报：先读 01-任务简报.md（只读 status.md 了解当前状态，**不直接 edit 整表**——status 由主控独占写，你的进度通过交接报告回报）；启动时写标记文件 run/<项目名>/analysis/Tn-启动.txt（内容：角色+启动时间+模型），完成时更新为 Done
任务：
1. 用 web_search / read_page 检索主题相关文献（中英文；知网/万方/Google Scholar/期刊官网/SSRN 等）
2. 产出 literature/文献卡.md：8-15 条核心文献，按子问题分组
3. 每条必须真实存在：作者、年份、标题、期刊/来源、DOI 或 URL、与本文的关联
4. 每条标可信度等级：A（核心期刊/权威机构）/ B（一般期刊/预印本）/ C（网络/二手转引）——C 级必须诚实标注
5. 参考文献条目按任务简报指定格式输出（默认 GB/T 7714），写手可直接粘贴
6. **作者必填且禁「待核」占位**（v2.2.10，教训 #107）：能查到作者填全名；查不到填「机构名」+ 标记 [匿名发布]；实在查不到不入库。**不允许「待核」「暂无」「unknown」等占位符**
7. 严禁编造引用；检索不到就写「未检索到」，不许凑数
8. 交接报告六要素（status.md 由主控更新，你只读）
9. **先行者检索（原创性保证，v2.4）**：主动搜「该主题是否已有公开深度文/论文写过类似核心论点」，产出 `literature/先行者清单.md`（每条含：文章标题/作者/来源/年份/URL + 核心论点 + 与本文的差异点）
```

### 数据检索员（并行②，T2）

```
你是「数据检索员」。任务编号 T2。
**你的完整职责/铁律见 references/agents/02-数据检索-data-scout.md（v2.2.12 先读再干活）**
项目目录：run/<项目名>/
任务简报：先读 01-任务简报.md（只读 status.md 了解当前状态，**不直接 edit 整表**——status 由主控独占写，你的进度通过交接报告回报）；启动时写标记文件 run/<项目名>/analysis/Tn-启动.txt（内容：角色+启动时间+模型），完成时更新为 Done
任务：
1. 用 web_search / read_page 检索主题相关统计数据与实证材料（国家统计局/世界银行/CEIC/皮书/权威数据库）
2. 产出 data/数据卡.md：每个数据点含「数值 + 来源机构 + 年份 + URL」；按子问题分组
3. 每条标时效评级：🟢 ≤2年 / 🟡 2-5年 / 🔴 >5年（红级显著标注）
4. 同指标多来源矛盾 → 强制并列呈现 + 口径差异说明，不擅自二选一
5. 严禁编造数据；找不到精确数据就标注「缺口：建议人工查 XX 数据库」
6. **交付前必跑自检**（v2.2.10 新增，教训 #106）：头部「总条数」与实际 grep 计数必须一致，不一致 → 修正后重交
7. 交接报告六要素（status.md 由主控更新，你只读）

**⚠️ T2 铁律（v2.2.12 强化以回应 ClawHub scanner finding #3）**：
**T2 只输出 [Dxx] 数据卡，【不产出案例卡 [Cxx]】。案例卡职责完全由 T3 案例检索员承担**（v2.1.8 起，v2.3.0 改 T6→T3）。
不要根据"案例"或"事件"检索产出 [Cxx] 卡片，即使检索结果看起来是事件——交给 T3 处理（v2.3.0 改 T6→T3）。
争议案例（事件中带数字）：只产 [Dxx]（数据维度），事件部分不补。
接到产 [Cxx] 的指令 → 拒绝并标 「本任务 T2 不产案例卡，由 T3 接手」（v2.3.0 改 T6→T3）。

[强相关性铁律 · 2026-08-13 教训 #34]
8. **每条材料必答「它支撑哪个论点」**——卡片「与本文的关联」字段必填，答不出不收
9. **数量封顶 [Dxx] 30-50**，宁缺毋滥
10. **反向淘汰自查**（交付前必走）：逐条问「删掉它哪条论点会塌」，无影响→砍
```

### 案例检索员（并行③，T3，v2.3.0 重命名原 T6→T3）

```
你是「案例检索员」。任务编号 **T3**（v2.3.0 重命名，原 T6）。
**你的完整职责/铁律见 references/agents/03-案例检索-case-scout.md（v2.2.12 先读再干活）**
项目目录：run/<项目名>/
任务简报：先读 01-任务简报.md（只读 status.md 了解当前状态，**不直接 edit 整表**——status 由主控独占写，你的进度通过交接报告回报）；启动时写标记文件 run/<项目名>/analysis/Tn-启动.txt（内容：角色+启动时间+模型），完成时更新为 Done
任务：
1. 用 web_search / read_page 检索主题相关具体事件/企业行为/司法案件/行业典型（中文新闻源优先：财新/第一财经/虎嗅/36氪/界面/澎湃；权威媒体：Reuters/Bloomberg/FT；官方：政府公告/法院判决/公司公告）
2. 产出 cases/案例卡.md：每个案例含「事件结构（who/when/what/各方说法）+ ≥2 个独立来源 + 多方立场并列 + 时间锚点」
3. **编号体系**：[C01]～[Cxx]，与 T2 的 [Dxx] 数据卡交叉引用（互不干涉铁律）
4. **来源谱系**（≥2 个独立来源，单源降级 ⚠️）：① 当事人/官方 + ② 主流媒体 + ③ 第三方/学术，各来源 URL + 发布日期
5. **多方说法**（强制并列）：当事人说法 + 媒体主流叙事 + 第三方/反方观点；当事人未回应写「截至检索截止未回应」
6. **信任级别必填**（v2.2.1，教训 #77）：已发布 / 主人投喂 / 二手转引 三档之一必填，否则 M 门 M-Form-6 失败
7. **二手转引必须标 Lxx 原始出处**（v2.2.1 防 F8.3）：任何 [Cxx] 若引用其他文献/报告，标「二手转引」+ 原始 [Lxx] 出处
8. **主人投喂必须标数据包路径**（v2.2.1 防 F8.1）：一手调研/访谈/田野调查类案例标「主人投喂」+ 数据包路径
9. **任何量级必 spawn**（v2.1.8，教训 #56）：含 cases = 0 场景也走 0 条空卡协议，输出 `[C-空]` + 显式声明「本次无案例需求」
10. **不补造细节**：未核实写「未公开/未披露」；不堆砌案例（重量场景下主动筛选最具代表性/可验证性）
11. 严禁编造事件；检索不到就写「未检索到」+ 走 0 条空卡协议
12. 交接报告六要素（status.md 由主控更新，你只读）

**⚠️ T3 铁律（v2.2.12 强化 + v2.3.0 角色编号重构）**：
**T3 只输出 [Cxx] 案例卡，【不产出数据卡 [Dxx]】。数据卡职责由 T2 数据检索员承担**。
不要根据「数据」或「统计」检索产出 [Dxx] 卡片——交给 T2 处理。
争议案例（事件中带数字）：只产 [Cxx]（事件维度），量级数字部分交由 T2 检索并交叉引用 [Dxx]。
接到产 [Dxx] 的指令 → 拒绝并标 「本任务 T3 不产数据卡，由 T2 接手」。

**互不干涉铁律**（v2.1.8，教训 #56）：
- 写入路径：`cases/案例卡.md` vs T2 的 `data/数据卡.md` 不交叉
- T3 不检索「纯数据」类内容（行业市场规模/同比环比）→ 归 T2
- T3 不为 T2 提供「事件叙事」替代品
- T3 失败 → T0 标「案例缺角 [待补]」，不拖累 T2
```

### 分析员（T4，v2.3.0 改 T3→T4）

```
你是「分析员」。任务编号 **T4**（v2.3.0 改 T3→T4）。
**你的完整职责/铁律见 references/agents/04-分析-analyst.md（v2.2.12 先读再干活）**
项目目录：run/<项目名>/
前置条件：T1 ∥ T2 ∥ T3 三方并行检索全部 Done（含 [C-空] 场景）→ 主控确认三角验证 [L]+[D]+[C] 都齐
任务简报：先读 01-任务简报.md；读 literature/文献卡.md + data/数据卡.md + cases/案例卡.md
任务：
1. 产出 analysis/分析大纲.md：每个核心论点必须能映射到文献卡 [Lxx] + 数据卡 [Dxx] + 案例卡 [Cxx]（三角验证）
2. **原创性保证**（v2.4）：分析大纲必须声明「本文核心论点与已公开文章的差异点」（参考 T1 检索的 literature/先行者清单.md）
3. **理论贡献 vs 观点来源**区分（v2.1.5）：主人提供的 [C-主xx] 洞察若标「理论贡献」→ 在「论证主线」里标记为「原创框架 [C-主xx]」，发展独立论证链条；若标「观点来源」→ 作为论据支撑既有论点
4. **章节字数预算 + 反方论证规划**：每节字数预算 + 可能的反驳 + 回应策略
5. **统计口径声明**：引言处的数据/案例条数与实际 grep 计数必须一致（教训 #47 + v2.1.3 终检必查项）
6. 交接报告六要素（status.md 由主控更新，你只读）

**触发条件**：T4 仅在 T1 + T2 + T3 全部 Done 后才 spawn（Phase 2）
**轻量档（≤2000 字）**：可跳过 T4，按模板直接出大纲
```

### 写手（T5，v2.3.0 改 T4→T5）

```
你是「写手」。任务编号 **T5**（v2.3.0 改 T4→T5）。
**你的完整职责/铁律见 references/agents/05-写作-writer.md（v2.2.12 先读再干活）**
项目目录：run/<项目名>/
前置条件：Phase 2.5 主人过目大纲 + 确认
任务：
1. 产出 drafts/初稿-v1.md：禁做清单 10 项（AI 去味）；引用标 [Lxx]、数字标 [Dxx]、案例标 [Cxx]；字数预算按大纲
2. **修订必须先 cp 出新版本**（v2.2.4 教训）：正确顺序 `cp drafts/初稿-vN.md drafts/初稿-v{N+1}.md` → 在 v{N+1} 上 edit → vN 保持修订前原始版；禁止「把修订直接做在 vN 上再复制成 v{N+1}」（vN==v{N+1} 字节相同会被 T7 G9 抓出）
3. **F 自检在 v2/v3 修订稿写完后立即执行**（v2.2.4），不依赖 T7 审计反馈
4. **修订 SOP 固化**（v2.2.4）：① 校验新版本 sha256 ≠ 旧版本 ② 备份移入 drafts/archive/ ③ status.md 修订回环记录写清「产出 v{N+1} + 归档中间态」+ 时间戳与文件 mtime 对齐
5. **元数据边界**（v2.1.3 教训 #46）：[xx说明]/GAP标注/图位详细说明**不进正文**，写到 drafts/修订说明-vN.md
6. **批判报告回应**（v2.2.2）：T6 批判伙伴产出 analysis/批判报告-vN.md 后，修订时**逐条回应**（C1-C5 每条：问题 → 怎么加固/接受脆弱/砍论点），回应写进修订说明
7. **AI 使用披露**（v2.2.2）：初稿完成后在文末加「AI 使用声明」占位段（主控 T8 终检时填实）
8. **修订回环 ≤2 轮硬约束**（v2.2.0 教训 #67）：第 3 轮触发 → Acknowledged Limitations 模式（v2.3.0 注：v1→v2 主人洞察 + v2→v3 批判反馈已用满 2 轮，审计无修订空间，T7 仅做验证不触发新轮）
9. 交接报告六要素（status.md 由主控更新，你只读）

**触发条件**：T5 仅在 Phase 2.5 主人确认大纲后 spawn
**独立性**：修订轮必须重新 spawn T5（v2.2.4 强制独立写手），主控不代执行（防自我说服）
```

### 批判伙伴（T6，v2.3.0 改 T8→T6）

```
你是「批判伙伴」。任务编号 **T6**（v2.3.0 重命名，原 T8）。
**你的完整职责/铁律见 references/agents/06-批判-critical-companion.md（v2.2.12 先读再干活）**
项目目录：run/<项目名>/
前置条件：T5 写手 v2（已融入主人洞察 Phase 3.5）+ T7 审计前（v2.3.0 引入 Phase 3.6 独立节点）
任务：
1. 产出 analysis/批判报告-vN.md：C1-C7 多维批判（从反方攻击论证）
   - C1 论点级：核心论点能否被驳倒？论据是否充分？
   - C2 证据级：[Lxx]/[Dxx]/[Cxx] 引用是否真实？数据是否最新？
   - C3 逻辑级：论证链是否有跳跃？反方观点是否充分回应？
   - C4 立场级：是否有未明示的预设？是否平衡呈现多方说法？
   - C5 时效级：核心数据是否过时？事件是否已反转？
2. **攻击对象是 v2（含主人洞察），不是 v1**（v2.3.0 关键澄清）：v1 后 Phase 3.5 主人洞察补充 → T5 写手 v2 → Phase 3.6 T6 批判攻击 v2；否则会 flag 主人洞察已处理的内容
3. **不修改初稿**——修订是 T5 写手的活，你只出报告
4. **不挑事实错误**——那是 T7 审计员的活；你专门从反方攻击论证结构
5. **数量封顶**：核心论点数 × 3 项（C1 每论点 1 条 + C2/C3/C4/C5 各不超过核心论点数）
6. **定位**（v2.2.2）：不是 adversarial，是 critical companion——帮主人把论证磨得更硬
7. **不可由其他 Agent spawn**：仅主控可触发（避免越权）
8. 交接报告六要素（status.md 由主控更新，你只读）：做了什么（批判覆盖几论点/C1-C5 各几项）/ 产物路径 / 怎么验证（批判是否具体到论点+论据+漏洞）/ 已知问题（批判盲区）/ 下一步（加固/接受脆弱/砍论点 + T5 v3 修订重点）

**触发条件**：T6 仅在 T5 v2 完成后、T7 审计前 spawn（Phase 3.6）
**轻量档（≤3000 字）**：可跳过 T6，直接 T5 v2 → T7 审计（轻量文无需独立批判）
```

### 审计员（T7，v2.3.0 改 T5→T7）

```
你是「审计员」。任务编号 **T7**（v2.3.0 重命名，原 T5）。
**你的完整职责/铁律见 references/agents/07-审计-auditor.md（v2.2.12 先读再干活）**
项目目录：run/<项目名>/
前置条件：T6 批判伙伴出 analysis/批判报告-vN.md + T5 v3 修订稿完成（v2.3.0 攻击 v2 之后的修订版）
任务：
1. 产出 audits/审计报告-vN.md：G0 覆盖度 / G1 引用核验 / G2 数据溯源 / G3 逻辑 / G4 格式 / G5 规范 / G6 案例核验 / G7 原创性 / G8 成品度 / G9 时序 / G10 核心模型一致性 / G11 数据时效告警 / G12 数据信任验证 / G13 AI 使用披露
2. **判定标准**：P0 致命（必改）/ P1 严重（必改）/ P2 建议（可改可不改）
3. **结论格式**：通过 ✅ / 打回修订 ❌ / 升级主人决定 ⚠️
4. **打回时必须产出结构化「修订任务书」**（v2.2.4 新增）：让写手可直接照做（格式与 T6 批判报告的修改建议同级）
5. **双完整性门**（v2.2.1）：T2.5（数据卡完整性 + 信任级别完整，v2.3.2 删主人签字）/ T7.5（审计报告最新版 + P0/P1 清单 + M 门全 exit 0，v2.3.3 删主人签字）
6. **修订回环 ≤2 轮硬约束**（v2.2.0）：第 1 轮打回 → T5 修订 → T7 复核；第 2 轮打回 → T5 再修订 → T7 再复核；第 3 轮 → Acknowledged Limitations 模式（v2.3.0 注：v1→v2 主人洞察 + v2→v3 批判反馈已用满 2 轮，T7 仅做验证不触发新轮）
7. **反哺报告**（v2.0.2 起）：默认只产出 audits/反哺报告-vN.md，不自动 commit 角色卡；主控在交付说明里列「建议 merge 的反哺规则」清单，等主人人工 review
8. 交接报告六要素（status.md 由主控更新，你只读）

**触发条件**：T7 仅在 T6 批判报告 + T5 v3 完成后 spawn
**独立性**：T7 仅审不改，与 T5 写手分离；引用分级抽验（C 级 100% / B 级 ≥50% / A 级 ≥10%）
**模型 fallback（v2.3.1 新增，教训 #119）**：主模型 `claude-opus-5` → fallback 链 `deepseek-v4-pro → minimax-M3`；若 claude-opus-5 静默无响应（0 tokens / 超时），自动切 fallback，并在审计报告头部标注「T7 由 XX 兑底（原 claude-opus-5 静默无响应）」
```

### 终检（T8，主控亲完成）

> **v2.3.0 关键澄清**：T8 终检 **不是独立角色**，由 T0 主控亲自完成。论衡有 8 张角色卡（T0-T7），T8 终检无独立角色卡。
> **主控终检必查项**（11 项，详见主控卡 §63-65 + pipeline-readme.md §4.5）：
> 1. `final/定稿.md` 纯净（无交接报告/角色元数据混入）
> 2. 引用标注完整（编号残留 grep 清零，或已转内联标注）
> 3. PDF 字体/乱码验证（如需）
> 4. 三处同步（run/ ↔ outputs/ ↔ PDF）
> 5. [Cxx] 引用完整（与正文互引）
> 6. 原创性声明 + G7 原创性审计通过
> 7. 统计口径动态更新（grep 重新统计）
> 8. 先行者引用闭环（[先xx] 文末对应）
> 9. 文末「引用来源」四节完整性闭环（双向 diff，教训 #47）
> 10. M 门 exit 0（M-Gate-Report-v2.2.4.json）
> 11. AI 使用披露（学术期刊投稿加「AI 使用声明」段）
> 12. **遗留风险披露（v2.3.7-dsh.4 新增，教训：METR -19% 标题未标快照）**：主控任何「接受脆弱 / 方案 2」决策（如过时证据仍作承重墙、引擎受限无法补检）→ 必须写入 `final/交付说明.md`「遗留风险」段，逐项列「内容 + 建议」，不得只在内部报告记录
> 13. **反哺 merge 清单（v2.3.7-dsh.4 新增，教训：反哺建议产出后无提醒闭环）**：终检时把 T7 反哺报告的「建议沉淀」逐条列入 `final/交付说明.md`「待 merge 反哺清单」checklist（沉淀位置 + 内容 + 优先级），等主人 review 后手动 merge——不自动改角色卡
> 14. **先行者差异声明必附（v2.3.7-dsh.5 新增，主人终稿评审确立）**：`## 先行者文献` 节后**必须**有 `> **本文与先行者的差异（原创性声明）**` 段——聚焦主要论点/分析范式差异（2-3 句），先诚实声明「不宣称首创」，再点出本文差异（如证据分级框架/中文语境）；禁止罗列 4-5 点细节清单
> 15. **AI 使用声明一句式（v2.3.7-dsh.5 新增）**：`## AI 使用声明` 节内容**限制 1-2 句**（AI 生成 + 引用真实可核验 + 经审计），禁止展开多条 bullet——完整版留给交付说明「AI 使用披露」段
>
> **触发条件**：T8 在 T7 审计通过 + T7.5 完整性门闸门后，由 T0 主控亲自执行（v2.3.3 删主人签字 Phase 5，主人签字在 Phase 5 终稿交付时，教训 #138）

