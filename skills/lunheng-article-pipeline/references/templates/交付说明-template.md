# 交付说明 — <项目名>

> 版本：v18.11.0（DSH bundle 插件）
> 位置：`run/<项目名>/final/交付说明.md`　｜　产出者：**T8 终检（主控亲执行）**
> 用途：**操作员报告**，给主人复核用。12 个固定字段见 [`../deliverables.md`](../deliverables.md) § 交付说明模板字段——**机检 M-Exist-7 逐字段核验**「字段齐备 + 有内容」，不得改名、不得省略、不得留 `<...>` 占位符。（v18.0.5 更正：本节旧版写 11，而模板实际 12 节，见 `deliverables.md:77`「由 11 显式化为 12」）
> **边界**：本文件不进入 `final/定稿.md`（论文），定稿也不得反引本文件内容（隔离由 M-Integrity-2 步骤 6 核验）。
>
> **⚠️ 增补段的写法（v18.11.0 F-9 反哺）**：允许在 12 字段之外**追加**说明段（如「字数统计」「模型路由」「反哺清单摘要」），但有两条硬约束——**不得改名、不得省略、不得打乱**既有 12 个二级标题（`## 1. 路径` … `## 12. 终检结论`），也不得把追加段插在 12 节序列**中间**（否则 M-Exist-7 按标题序号匹配会错位，判「字段缺失」）。**追加段一律放在 §12 之后**，形如 `## 附 1. 字数统计`。实测教训：本项目把「字数统计」插成 §3（挤掉原 §3 遗留风险）→ M-Exist-7 报「字段归属错位」。

---

## 1. 路径

| 项 | 路径 |
|----|------|
| 定稿 | `run/<项目名>/final/定稿.md` |
| 图件 | `run/<项目名>/final/图件/`（<N> 张） |
| 证据包 | `run/<项目名>/final/证据包/` |
| 审计报告 | `run/<项目名>/audits/审计报告-v<N>.md` |
| 审稿报告 | `run/<项目名>/audits/审稿报告-v<N>.md`（未启用 T9 则写「未启用」） |

## 2. 图件清单

| 图号 | 标题 | 数据来源编号 |
|------|------|------------|
| 图1 | <标题> | <[Dxx] / [Cxx] / 「示意，无数据出处」> |

## 3. 遗留风险

- **接受脆弱的论点**：<论点 → 原因 → 剩余风险>（无则写「无」）
- **修订能力上限**：<修订轮次已达上限 / 未达上限>

## 4. 人工核验项

- <付费墙文献、反爬源、红级数据等待人工复核条目；无则写「无」>

## 5. 数据溯源 check-list

- [ ] 付费墙文献（仅核验摘要，待回查全文）：<清单>
- [ ] 二手转引（待回查原始 [Lxx] 出处）：<清单>
- [ ] 🔴 红级数据（>5 年，待核时效）：<清单>
- [ ] 待核原刊信息（DOI/卷期/页码待补）：<清单>

## 6. 成本指标

> **⚠️ 本段须落脚本实值（`~NN[MKB]` 格式，如 `~5M`/`~250K`），不得以定性描述替代（v18.2.5 强化，主控实战反哺 P2；v18.6.3 看板反馈：原写法缺格式示例导致 17/21 项目看板 token 列空）**
> **实测教训（本项目 ai-cad-cam-impact）**：主控全程**未跑** `token-cost.mjs`（因 Phase 0 未收集会话 ID），本段只能写「已耗：12 次 spawn + 4 轮机械编辑」这类定性描述——**字段齐备但无实测值**；M-Exist-7 只核「字段有内容」，核不出这一层。
> **落地要求（两条）**：
> ① **Phase 0 起记录会话 ID**：主控每 spawn 一个角色，把其 agent ID 追加到 `run/<项目名>/model-routing.md` 的「会话 ID 收集」栏（终检时一次性喂给脚本）；
> ② **T8 终检必跑** `node scripts/token-cost.mjs --sessions <主会话ID>,<ID…> [--top 3]`，把摘要贴入本段（**实测值必须 `~NN[MKB]` 格式**）；脚本不可用（如无会话缓存）→ **如实标注「实测不可得 + 原因」**，不得静默留空、不得用估算充当实测。

- **token（实测）**：<`node scripts/token-cost.mjs --sessions …` 输出摘要，**实测值必须 `~NN[MKB]` 格式**（如 `~5M`/`~250K`/`~1.2M`）；不可得则写「实测不可得：<原因>」>
- **时长**：<墙钟时长>
- **模型组合**：<各 T 角色实际模型 + fallback 记录；引用 `run/<项目名>/model-routing.md`>
- **最贵角色（实测）**：<`--top 3` 结果，含 cacheRead 排名；不可得则写「实测不可得：<原因>」>
- **会话 ID 收集栏**：`run/<项目名>/model-routing.md`（Phase 0 起逐条追加，供本段实测）

## 7. 建议 merge 的反哺清单

- [ ] <反哺规则 → 目标角色卡>（等主人 review 后**手动** merge，不自动 commit）

## 8. AI 使用披露（完整版）

- **AI 生成段**：<哪些章节由 AI 生成>
- **主人洞察出处**：<[C-主NN] 清单>
- **检索证据**：<文献/数据/案例卡数量与信任级别分布>
- **模型 fallback 记录**：<发生过的回退及处置>
- **教训沉淀**：<本轮新增教训>

### 8.1 修订轮次记录（v18.10.0 战略反哺新增）

> **A 轨审计打回轮**：≤2 轮硬约束（T7 打回 P0/P1 → T5 修订 → T7 复核；第 3 轮触发 Acknowledged Limitations 模式）
> **B 轨深化轮**：至多 +1 深化轮（v18.10.0 从历史「不限额」收紧为「至多 +1」——见 pipeline-readme.md §6.6.1）

| 轮次 | 类别 | 来源 | 触发时间 | 主要修订 | 关闭状态 |
|------|------|------|---------|---------|---------|
| v1 | A 轨 / 初稿 | Phase 3 写手 | <时间> | <主要修订> | / |
| v2 | A 轨 / B 轨 | <打回/洞察/批判/审稿/G14> | <时间> | <主要修订> | 关闭 / 部分 / 未关闭 |
| v3 | A 轨 / B 轨 | <同 v2> | <时间> | <主要修订> | 关闭 / 部分 / 未关闭 |
| v4 (如有) | A 轨 / B 轨 | <同 v2> | <时间> | <主要修订> | 关闭 / 部分 / 未关闭 |

> **B 轨深化轮字段填充（v18.10.0 战略反哺新增）**：当本轮为 B 轨时，**必须**额外记录：
> - **来源类目**：<主人洞察 / T6 批判 / T9 审稿 / G14 终闸>（必填）
> - **实质建议摘要**：<3-5 行概括性内容>（必填）
> - **本轮交付增量**：<深化轮带来 vs A 轨未触发时的差异>（必填）
> - **未采纳项与原因**：<深化建议中 T5 评估不采纳的部分 + 理由>（如有则填）

> **本次机制文件改动依据主人显式授权**（AGENTS.md 例外条款适用）。

## 9. 证据包指纹

- **sha256**：`[哈希校验待主人回填]`
  > 占位符即为合规（主人在 host shell 手动计算后可选回填真实哈希；agent 不执行 sha256，见 M-Integrity-1 步骤 7 / M-Integrity-2 步骤 4）。

## 10. 投稿就绪检查表

- **推荐期刊 + 匹配度**：<Top 3 摘要；未启用期刊匹配则写「未启用」>
- **格式转换**：<Word / PDF 导出状态>
- **AI 使用声明**：<三套（期刊要求 / 单位要求 / 通用）状态>

> 完整表见 [`../templates/投稿就绪检查表-template.md`](../templates/投稿就绪检查表-template.md)。

## 11. 主人决策记录

- **四门时间线（一行制）**：`Phase 0 <HH:MM> <通过/驳回>｜Phase 2.5 <…>｜Phase 3.5 <…>｜Phase 5 <…>`
  > 原始依据 = `run/<项目名>/阶段确认-<阶段>.md` 的 §6「主人回复」（主控回填的原话）。
  > **某门缺 §6 回填** → 此处如实标注「该门决策**未留痕**」，并在终检结论说明——**不得代填**。

### 11.1 四声明 checklist（v18.10.0 战略反哺新增 / 学术伦理声明联动）

> **本节义务**：学术论文须确认 `final/定稿.md` 文末四声明节齐全 + 顺序正确（M-Form-7 文末五节扩面到九节）+ 档位勾选（M-Form-12 子门）+ 占位符已填实（M-Form-3）。

| # | 声明 | 模板路径 | 是否触发 | 档位勾选 | 占位符已填实 | 文末节顺序号 |
|---|------|---------|---------|---------|------------|------------|
| ① | CRediT 作者贡献声明 | [`./CRediT-作者贡献声明.md`](./CRediT-作者贡献声明.md) | □是 / □否 | □已勾选 | □已填实 | 第 5 节 |
| ② | COI 利益冲突声明 | [`./COI-利益冲突声明.md`](./COI-利益冲突声明.md) | □是 / □否 | □已勾选 | □已填实 | 第 6 节 |
| ③ | 数据可用性声明 | [`./数据可用性声明.md`](./数据可用性声明.md) | □是 / □否 | □已勾选 | □已填实 | 第 7 节 |
| ④ | 伦理审批声明 | [`./伦理审批声明.md`](./伦理审批声明.md) | □是 / □否 | □已勾选 | □已填实 | 第 8 节 |
| ⑤ | AI 使用声明（已有） | [`./AI-使用声明-template.md`](./AI-使用声明-template.md) | □是（默认） | □已勾选 | □已填实 | 第 9 节 |

**机检判别（v18.10.0 起扩面，文末节扩到九节）**：

- M-Form-7 文末白名单（v2.3.5）扩面到九节顺序断言：档位未勾选 / 顺序错误 / 占位符残留 → T7 人工判 P1（**机检硬门待 v18.10.0 后续补丁实现**，本批次仅声明契约与人工核验流程）
- **本文档 §11.1 任一行「未勾选」 → T8 终检必须如实标注「本文不适用」+ 理由**，不得代填

> **本次机制文件改动依据主人显式授权**（AGENTS.md 例外条款适用，用户原话「请你依次修订吧」即授权）。

## 12. 终检结论

- **M 门**：`final/M-Gate-Report.json` exit = <0 / 1 / 2 / 3>（<项数统计>）
- **闸门**：T2.5 / T7.5 均 ✓（见 `audits/闸门记录-T2.5.md` / `audits/闸门记录-T7.5.md`）
- **T8 签字**：<主控签字结论；M-Form-7 exit 0 后才可签>
