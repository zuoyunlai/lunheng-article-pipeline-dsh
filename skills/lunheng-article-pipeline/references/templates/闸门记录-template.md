# 阶段闸门记录 — <项目名>

> 版本：v18.5.1（DSH bundle 插件）
> 用法：**抄两份**，各自只保留对应闸门那一段 → `audits/闸门记录-T2.5.md`、`audits/闸门记录-T7.5.md`
> 产出者：**主控（T0）**，在闸门判定**当场**写（不得事后补写；补写的记录失去证据价值）
> 用途：把「主控 LLM 兜底执行」的两道闸门变成**可核对的结构化记录**——机检 **M-Exist-5** 逐项核验：检查项齐备 / 实据是机械证据 / 结论词固定 / 判 ✗ 的行写了原因。

> **为什么需要这张表**：T2.5 与 T7.5 此前只有伪代码，**没有任何落盘表单**——闸门过没过、依据是什么只留在会话里，换会话或事后审计就不可追溯；M-Integrity-2 的判定也没有可核对的输入。本表把闸门从「主控说过了」变成「有据可查地过了」。

## 结论图例

| 结论词 | 含义 |
|--------|------|
| ✓ | 该项通过 |
| ✗ | 该项未通过（**必须在「失败原因」列写明**，并按严重度决定是否放行） |
| N/A | 本项目不适用（如未启用配图 / 无修订轮；需在失败原因列写一句理由） |

> **「实据」列的硬要求**：必须是**机械证据**——文件路径、脚本 exit code、执行的命令、sha256 值。
> 写「已检查」「已核对」「确认无误」这类自述 = 机检判失败（闸门留机械证据，见 `AGENTS.md`）。
>
> ⚠️ **检查项名必须与脚本 gate 标签逐字一致（v18.2.6 审计修复）**：本表曾有两行写「信任级别一致性（M-Exist-3）」——而 `m-gate-check.mjs` 的 `M-Exist-3 引用闭环` **只做正文 `[Dxx]` ↔ 数据卡条目对账，不查信任级别**（信任级别由 **M-Form-6** + **G12** 承担）。那两行构成**假绿**：本表要求「实据」列给路径/exit code，于是它**会被 M-Exist-5 机检、却没有任何检查在跑**。已按脚本实际标签改名。**新增/改检查项前先看 `m-gate-check.mjs` 的 `gate:` 值**（22 项逐字清单见 `../_shared/M-Gate-Algorithm-appendix.md` §1.2）。

## T2.5（数据完整性门：T2 数据检索 → T4 分析前）

| 检查项 | 实据（路径 / exit code / 命令） | 结论 | 失败原因（判 ✗ / N/A 时必填） |
|--------|------------------------------|------|---------------------------|
| 数据卡文件存在 |  |  |  |
| 数据条目数（双格式并集去重） |  |  |  |
| 任务简报数据需求总数 |  |  |  |
| 数据条目数 ≥ 需求总数 |  |  |  |
| 信任级别完整性（M-Form-6） |  |  |  |
| 引用闭环（M-Exist-3：[Dxx] 正文 ↔ 数据卡条目） |  |  |  |
| 数据卡头部声明 vs 实际计数 |  |  |  |
| 证据包哈希占位符（可选验证） |  |  |  |

> **v18.2.6 更名兼容说明**：旧行名「信任级别一致性（M-Exist-3）」措辞与实际机检不符（脚本只做引用闭环对账，不查信任级别）——**M-Exist-5 按「该行引用的检查编号」归一**，故历史闸门记录里的旧措辞仍被承认，**不必回填历史项目**；新建闸门记录请用上表的新行名。

## T7.5（交付完整性门：T7 审计 → T8 终检前）

| 检查项 | 实据（路径 / exit code / 命令） | 结论 | 失败原因（判 ✗ / N/A 时必填） |
|--------|------------------------------|------|---------------------------|
| 审计报告最新版存在 |  |  |  |
| P0/P1 清单已列 |  |  |  |
| M 门全部 exit 0 |  |  |  |
| 证据包指纹占位符 |  |  |  |
| 引用闭环（M-Exist-3：[Dxx] 正文 ↔ 数据卡条目） |  |  |  |
| 论文交付物 vs 报告独立隔离 |  |  |  |
| 修订轮由独立写手执行 |  |  |  |

## 闸门判定

- **整体结论**：✓ 通过（派发下一阶段） / ✗ 不通过（不派发，先修）
- **严重度**：任一子项 P0 → 整体 P0（不派发）；仅 P1 → 可放行但须在 `01-任务简报.md`「已知风险」段与交付说明如实记录
- **时间**：<YYYY-MM-DD HH:MM>　｜　**执行者**：主控（T0）
