# 版本升级自审门（Upgrade Self-Audit Gate）v2.3.0（含 v2.3.1 增量门 U，总门数 20）

> **v2.3.1 升级（2026-08-21 教训 #119，v2.3.0 首次实战后）**：
> - **新增门 U（模型健康度预检）**：v2.3.0 首次实战 T7 审计员 claude-opus-5 静默无响应（kkaiapi 接口 0 tokens），无显式 fallback 链。门 U 机械化检查 T7 卡 fallback 链 + pipeline-readme 预检 + 标注三件套。
> - **门数**：19 → 20 门。

> **v2.3.0.2 升级（2026-08-21 14:03 第三方独立审计后）**：
> - **新增门 S（scripts/ + CI 元元文档扫描）**：14:03 第三方审计发现 scripts/check-version.sh + 版本号同步脚本 角色文件名仍是 v2.2.x 旧名（运行直接报错）。这是「元元文档盲区」——17 门自审门只管 references/ 下文档，不管 scripts/ + CI。教训 #118.1。
> - **新增门 T（负向验证——组合残留双向扫描）**：17 门自审门是「正向验证」（该有的都有），缺「负向验证」（不该有的都没有）。perl 替换连带误伤（T1/T2/T4）+ 同一文件部分命中（F8 改了 F9 漏了）都是负向盲区。本门对每个 v2.2.x→v2.3.0 映射做双向 grep。
> - **门数**：17 → 19 门。

> **v2.3.0 升级（2026-08-21）**：
> - **角色编号自检升级**：原 8 角色 → v2.3.0 9 角色（T6 案例检索 → T3 案例检索，T3-T8 顺延，T7/T8 交换位置：终检 → T8 主控亲完成、批判 → T6 独立早期攻击、审计 → T7 形式审查）。门 A/B/C/D 全部按新编号走。
> - **背景**：v2.3.0 重构源自教训 #116（README 内部 Phase 3.5/3.6 冲突 + 角色编号重构合并修复），是论衡 8 个月迭代以来最大的一次重构。教训 #115 沉淀的「全文 grep 关键词矩阵」+ 「派发话术实时读取不复制」+ 「能力边界前置到 SKILL.md 顶部」三防线在本版本升级中被严格遵守。
> - **关键变化**：新增「v2.3.0 角色编号重构专项自审」——门 H（9 角色编号一致性，grep T1-T8 在 5 个真源层各 ≥3 次）+ 门 I（Phase 3.6 引入一致性，grep SKILL.md/pipeline-readme/README/设计文档 4 文件 Phase 3.6 段都有）+ 门 J（双端 md5 校验，论衡工作区 vs ClawHub 副本 8 张角色卡 md5 完全一致）。
> - **v2.3.0.1 升级（2026-08-21 教训 #118 第 8 次连发）**：主人在 11:46「整体检查所有文件」打脸发现 _shared/ + deliverables.md + 修订回环段 + 设计文档 等元文档漏改。**新增 5 门**：
>   - **门 K（user-facing 文档关键词矩阵）**：README/QUICKSTART/glossary/PERFORMANCE-PROFILE 4 文件 grep v2.3.0 + T6→T3 + Phase 3.6 + 九角色 等关键词 ≥3 次
>   - **门 L（互引用一致性）**：SKILL.md §87 ↔ pipeline-readme.md#派发话术 等跨文档锚点必须存在 + 内容真实
>   - **门 M（templates T 编号一致性）**：13 个 templates/*.md T 编号 + 子编号（T2.5/T7.5/T7.2）全部按 v2.3.0
>   - **门 N（_shared/ 协议文档全量扫描）**：4 个当前生效 _shared 文件 T 编号 + 子编号 + 引用全按 v2.3.0；archive/ + 版本升级自审门-v2.2.6.md 历史快照保留原版（教训 #91）
>   - **门 O（_shared/ 子编号一致性）**：T2.5/T7.5/T7.2 子编号不允许残留 T5.5/T5.2
>   - **门 P（_shared/ archive vs 当前分离）**：archive/* + 版本升级自审门-v2.2.6.md 保留 v2.2.x 编号，当前生效文件必须 v2.3.0 化
>   - **门 Q（主控卡修订回环段 T 编号）**：主控 §220-280 修订回环段全用 v2.3.0 编号，无 T4 重写 / T5 审计打回等 v2.2.x 残留
>   - **门 R（deliverables / glossary / 设计文档 元文档扫描）**：3 类元文档必有 v2.3.0 重构说明段
> - **预期工作量**：v2.3.0 重构 commit 7 个 + v2.3.0.1 后续修复 commit 5+ 个；全部改后跑一遍本自审门 17 门（7 v2.2.6 + 3 v2.3.0 既有 + 5 v2.3.0 + 5 v2.3.0.1 新增 = 17），exit 0 才允许 tag + push。

> **v2.2.6 旧背景（保留供历史参考）**：

> **背景**：论衡版本升级反复踩「改 A 漏 B」（教训 #36/#58/#60）与「头痛医头」（教训 P1-3）。最典型的两例：
> - v2.2.2 新增 T6 批判伙伴 + v2.1.8 强化 T3 案例检索员时，**SKILL.md 正文三处（流水线全景/项目目录/派发话术）漏同步**，直到 v2.2.6 主人人工发现才补；
> - v2.2.4 改进只修了实战暴露的单点（M-Exist-1 内联空转），未系统排查同类问题（G2/M-Form-1/M-Exist-3/G4-2 全空转），v2.2.5 审计才补全。
>
> **解法**：把「版本升级」本身当作需要机械校验的对象——每次改角色卡/文档/机制，commit 前必须跑一道自审门，**不依赖人（或 LLM）自觉**，用 grep + md5 + ls 三件套机械化验证。
> **设计哲学**：与 M 门同源——「形式合规 ≠ 存在性合规」，版本升级的「改 A 漏 B」本质是「改了真源却漏了下游」。自审门就是版本升级的 M 门。

## 触发条件

以下任一动作发生时，主控必须跑自审门（**commit 前，不是 commit 后**）：

1. 新增/删除/重命名角色卡（`references/agents/0X-*.md`）
2. 修改角色卡机制（铁律/G 项/F 模式/M 门算法）
3. 升级版本号（SKILL.md version / README / 设计文档）
4. 修改真源层任一文档（README/设计文档/SKILL.md/`references/_shared/failure-modes.md`/模板）

> 纯项目产出（run/<项目名>/ 下的稿件）**不触发**自审门——自审门只管论衡机制本身，不管论文成品（论文成品走终稿 M 门）。

## 检查项（17 项，全机械化，exit 0 才 commit）

> **v2.3.0.1 起共 17 门**（7 v2.2.6 + 3 v2.3.0 + 5 v2.3.0 + 5 v2.3.0.1 新增）。门 K/L/M 已在「v2.3.0 升级说明」段文档描述但不在 17 门计数内（已合并入门 H/J/N）；门 N/O/P/Q/R 为 v2.3.0.1 新增的 5 门。

### 门 A：角色卡完整性（ls）
```bash
ls references/agents/ | grep -E '^0[0-8]-'
# 应有 8 张卡：00主控/01文献/02数据/03案例/04分析/05写手/06批判/07审计（v2.3.0 重构）
# 无 08 正常——T8 终检是主控职责，无独立角色卡（v2.3.0 重构明确）
# 数量 != 8 → FAIL
```

### 门 B：角色清单三处一致性（grep，防 T8/T6 漏同步）
```bash
# 提取 agents/ 目录实际存在的角色编号（01-08 中除 07）
ls references/agents/ | grep -oE '^0[0-9]' | sort -u > /tmp/agents_exist.txt
# 三处文档分别 grep 每个角色编号 T1-T8（含 T8 终检、T0 主控，v2.3.0 重构）
for f in "references/pipeline-readme.md" "references/../README.md" "SKILL.md"; do
  # 派发话术段 + 流水线全景段 + 项目目录段 是否覆盖每个角色
  grep -cE 'T[0-8]' "$f"
done
# 判定：README.md + SKILL.md 的「流水线全景」「派发话术」「项目目录」三处，
#       每个实际存在的角色编号都至少出现一次 → PASS；缺任一 → FAIL
```
**核心判据**：新增角色（如 T8）后，`grep -c 'T8' SKILL.md` 必须 ≥3（流水线全景 + 项目目录 + 派发话术三处各一次），否则就是「角色卡加了、正文漏了」。

### 门 C：版本号五处一致性（grep）
```bash
NEW_VER="v2.2.6"  # 本次升级的目标版本
# 五处真源必须都含 NEW_VER：
# ① SKILL.md version 字段（ClawHub 副本）
grep -E "^version:" ~/.DSH/workspace/skills/lunheng-article-pipeline/SKILL.md
# ② references/pipeline-readme.md 版本历史行
grep -c "$NEW_VER" references/pipeline-readme.md
# ③ pipeline/设计文档.md 版本历史段
grep -c "$NEW_VER" pipeline/设计文档.md
# ④ settings.yaml 论衡 agent description
grep -c "$NEW_VER" settings.yaml
# ⑤ `references/_shared/failure-modes.md` / MEMORY.md（如有版本段）
grep -c "$NEW_VER" `references/_shared/failure-modes.md` 2>/dev/null
# 任一为 0 → FAIL（该层漏更版本号）
```

### 门 D：双端 md5 一致性（教训 #57）
```bash
# workspace-paperwriter/pipeline/ ↔ ClawHub skill 副本/references/agents/ 对应文件逐一 md5 对比
SKILL=~/.DSH/workspace/skills/lunheng-article-pipeline
for f in agents/00-主控-coordinator.md agents/01-文献检索-literature-scout.md \
         agents/02-数据检索-data-scout.md agents/03-分析-analyst.md \
         agents/04-写作-writer.md agents/05-审计-auditor.md \
         agents/06-案例检索-case-scout.md agents/08-批判-critical-companion.md; do
  [ "$(md5sum pipeline/$f | cut -d' ' -f1)" = "$(md5sum $SKILL/references/$f | cut -d' ' -f1)" ] \
    || echo "MISMATCH: $f"
done
# 任何 MISMATCH → FAIL（真源改了、副本没同步，或反之）
```

### 门 E：真源五层同步（清单核对，v2.3.0.1 修）

> **v2.3.0.1 修**：原设计 ④ `description 含 NEW_VER` 太严格（实际 description 字符串含「v2.3.0：角色编号重构...」格式），改为 `description 含 v2.3.0` 即可。

```bash
NEW_VER="v2.3.0"
# ① 论衡工作区 git
git log --oneline -1
git tag --list | grep "$NEW_VER"
# ② 文档/角色卡
# ③ GitHub Web UI
gh repo view --json description 2>/dev/null | grep -q "$NEW_VER" || echo "  WARN: GitHub description 未含 v2.3.0"
# ④ settings.yaml description
grep -q "$NEW_VER" settings.yaml || echo "  FAIL: settings.yaml 缺 v2.3.0"
# ⑤ DSH 本地记忆 memory（待主人拍板）
```

| 层 | 验证点 | 命令 |
|----|-------|------|
| ① git | commit 存在 + tag 已打 | `git log --oneline -1` + `git tag | grep $NEW_VER` |
| ② 文档/角色卡 | 门 A-D 全过 | - |
| ③ GitHub Web UI | description/topics 已更新 | `gh repo view --json description` |
| ④ settings.yaml | description 含 v2.3.0 | 门 C ④（v2.3.0.1 修：去严格匹配）|
| ⑤ DSH 本地记忆 memory | entity memory 已 store | `文件记忆` 已调用（待主人拍板）|

### 门 F：新增机制落地检查（防「头痛医头」，教训 P1-3，v2.3.0.1 扩）

> v2.3.0.1 扩：除 v2.2.0 基础关键词外，新增 Phase 3.6 关键词（v2.3.0 引入独立节点）。

```bash
# v2.3.0 关键变更：9 角色编号 + Phase 3.6 + T8 主控亲完成 + T6 批判独立早期攻击
KEYWORDS=(
  'v2.3.0'
  'T3 案例检索'
  'Phase 3.6'
  'T6 批判伙伴'
  'T7 审计'
  'T8 主控亲完成'
  '原 T6→T3'
)

fail=0
for kw in "${KEYWORDS[@]}"; do
  cnt=0
  for f in pipeline/SKILL.md references/pipeline-readme.md pipeline/glossary.md pipeline/设计文档-架构.md pipeline/设计文档-哲学.md pipeline/pipeline-readme.md pipeline/QUICKSTART.md; do
    grep -qF "$kw" "$f" 2>/dev/null && cnt=$((cnt+1))
  done
  if [ "$cnt" -lt 4 ]; then
    echo "WARN: 关键词 '$kw' 在 7 文件中仅 $cnt 个命中（建议 ≥4）"
  fi
done
# 7 文件含 v2.3.0 + Phase 3.6 + T6/T7/T8 编号 → PASS
```

### 门 F-ext：glossary.md 必须显式含「Phase 3.6」段（v2.3.0.1 补）

```bash
# glossary.md §一 9 角色清单的 T6 批判伙伴段应含「Phase 3.6」表述
if ! grep -q "Phase 3.6" pipeline/glossary.md; then
  echo "WARN: glossary.md 未显式含 Phase 3.6 引用（建议在 §一 T6 批判伙伴段加一句「Phase 3.6 引入」）"
fi
```

### 门 G：关键词矩阵 grep（教训 #60 SOP）
```bash
# 5 层真源 × N 文件 × 关键词矩阵，全命中才算闭环。
# 例：本次升级关键词「批判伙伴」应 grep：
#   agents/08-批判-critical-companion.md（角色卡）+ references/pipeline-readme.md（派发话术/全景）
#   + SKILL.md（三处）+ 设计文档（版本历史）+ settings.yaml（description）
#   + ClawHub 顶层 README.md（GitHub 根目录精简版，树形结构/项目目录/流水线全景三处）⚠️ v2.2.6 补
# 任一文件漏关键词 → 该文件漏同步 → FAIL
```

> ⚠️ **v2.2.6 补丁（教训：主人发现顶层 README 漏 T8）**：真源清单必须包含 **ClawHub skill 副本顶层 `README.md`**（GitHub 仓库根目录直接显示的精简版，含树形结构 + 项目目录 + 流水线全景）。它是**独立真源**，不是 `references/pipeline-readme.md` 的副本——之前门 G 只 grep 了 references/pipeline-readme.md（完整版），漏了顶层精简版，导致 v2.2.2 加 T8 时顶层 README 三处（树形结构缺 T8 + T6 重复 + 目录缺 08 卡）一直没被发现，直到主人人工指出。

### 门 H：9 角色编号一致性（v2.3.0 新增，教训 #116）

```bash
# 5 个真源层 × 8 张角色卡 + T8 主控终检
for t in T1 T2 T3 T4 T5 T6 T7 T8; do
  cnt_skill=$(grep -c "\b$t\b" pipeline/SKILL.md)
  cnt_readme=$(grep -c "\b$t\b" references/pipeline-readme.md)
  cnt_pre=$(grep -c "\b$t\b" pipeline/pipeline-readme.md)
  cnt_design=$(grep -c "\b$t\b" pipeline/设计文档-架构.md)
  cnt_gloss=$(grep -c "\b$t\b" pipeline/glossary.md)
  total=$((cnt_skill + cnt_readme + cnt_pre + cnt_design + cnt_gloss))
  if [ "$total" -lt 5 ]; then  # 每个角色编号在 5 真源层平均 ≥1 次
    echo "FAIL: 角色 $t 在 5 真源层总命中 $total < 5（SKILL=$cnt_skill README=$cnt_readme pre=$cnt_pre design=$cnt_design gloss=$cnt_gloss）"
  fi
done
# 任一 T 角色在 5 真源层总命中 < 5 → FAIL
```

### 门 I：Phase 3.6 引入一致性（v2.3.0 新增）

```bash
# v2.3.0 新增 Phase 3.6（批判伙伴独立早期攻击 v2），4 文件必须都引入
for f in pipeline/SKILL.md pipeline/pipeline-readme.md references/pipeline-readme.md pipeline/设计文档-架构.md; do
  if ! grep -q "Phase 3.6" "$f"; then
    echo "FAIL: $f 缺 Phase 3.6 引入"
  fi
done
# 4 文件全含 Phase 3.6 → PASS
```

### 门 J：双端 md5（v2.3.0 新增，3 核心文档，v2.3.0.1 修）

> **v2.3.0.1 修**：原设计 5 文件双端 md5，但 SKILL.md / README.md 是仓库根独立文件（论衡工作区在 `pipeline/` 下没有这两个文件），它们与 `references/glossary.md` 等不在同一双端路径。修正为 3 文件双端 md5（只检查论衡工作区 `pipeline/` ↔ ClawHub `references/` 对应映射）。

```bash
SKILL=~/.DSH/workspace/skills/lunheng-article-pipeline
for f in glossary.md pipeline-readme.md 设计文档-架构.md; do
  ws=$(md5sum "pipeline/$f" 2>/dev/null | cut -d' ' -f1)
  ch=$(md5sum "$SKILL/references/$f" 2>/dev/null | cut -d' ' -f1)
  if [ "$ws" != "$ch" ]; then
    echo "MISMATCH: $f (ws=$ws, ch=$ch)"
  fi
done
# SKILL.md / README.md 仓库根独立双端（md5 不需一致——论衡工作区是完整版，仓库根是精简版）
# 仅检查 3 文件双端 md5 → PASS
```

### 门 J-bonus：仓库根 SKILL.md / README.md v2.3.0 标识（v2.3.0.1 补）

```bash
# GitHub 仓库根的 SKILL.md / README.md 是否含 v2.3.0 标识（无需 md5 一致）
for f in SKILL.md README.md; do
  if ! grep -q "v2.3.0" "$f"; then
    echo "FAIL: 仓库根 $f 缺 v2.3.0 标识"
  fi
done
```

### 门 K：user-facing 文档关键词矩阵（v2.3.0.1 新增）

> **背景**：主人在 10:29 + 10:45 + 11:01 三次打脸发现 README/QUICKSTART/glossary 头部「v2.3.0 当前版本」+ 树形图 + 概念段漏改。教训 #118 沉淀：user-facing 文档必须有显式 v2.3.0 标识。

```bash
USER_DOCS=(
  pipeline/SKILL.md
  references/pipeline-readme.md
  pipeline/QUICKSTART.md
  pipeline/glossary.md
  pipeline/`references/operations.md`
)
# 5 关键词（v2.3.0 + 4 个 v2.3.0 关键变更描述）
KEYWORDS=('v2.3.0' 'T6→T3' 'Phase 3.6' 'T8 主控亲完成' '九角色')

fail=0
for f in "${USER_DOCS[@]}"; do
  if [ -f "$f" ]; then
    cnt=0
    for kw in "${KEYWORDS[@]}"; do
      grep -qF "$kw" "$f" 2>/dev/null && cnt=$((cnt+1))
    done
    if [ "$cnt" -lt 3 ]; then
      echo "FAIL: $f v2.3.0 关键词命中 $cnt/5（需≥3）"
      fail=$((fail+1))
    fi
  fi
done
[ "$fail" -eq 0 ] && echo "✅ 门 K 通过：5 user-facing 文档 ≥3 关键词命中"
```

### 门 L：互引用一致性（v2.3.0.1 新增，教训 #57 升级）

> **背景**：v2.3.0 推送时 SKILL.md §87「T1/T2/T3/T4/T5/T6/T7/T8 八个角色完整派发模板见 references/pipeline-readme.md#派发话术」是**虚假引用**——pipeline-readme.md 实际只有 T1/T2 两个模板。这是「文件 md5 一致但内容不匹配」的典型问题。

```bash
# SKILL.md §87 声明的「8 角色派发模板」vs pipeline-readme.md 实际派发话术数
SKILL_CLAIM=$(grep -E "T1/T2/T3/T4/T5/T6/T7/T8.*派发模板" pipeline/SKILL.md | wc -l)
PRE_DISPATCH=$(grep -cE "^### (文献检索员|数据检索员|案例检索员|分析员|写手|批判伙伴|审计员|终检)" pipeline/pipeline-readme.md)

if [ "$PRE_DISPATCH" -lt 5 ]; then
  echo "FAIL: SKILL.md §87 声明 8 角色派发模板，但 pipeline-readme.md 实际只有 $PRE_DISPATCH 个派发话术"
  echo "  → 主人 11:08 打脸修复时已补齐 7 角色派发话术，确认修复后此门通过"
fi
[ "$PRE_DISPATCH" -ge 5 ] && echo "✅ 门 L 通过：派发话术 ≥5 个"
```

### 门 M：templates T 编号一致性（v2.3.0.1 新增，教训 #118 第 6 次连发）

> **背景**：v2.3.0 推送时 13 个 templates/*.md 漏改 v2.2.x 编号（案例卡 T6→T3 / 数据卡 T5.5→T7.5 等），主人在 11:19 打脸。

```bash
TEMPLATES=(
  templates/案例卡-template.md
  templates/案例卡-template-lite.md
  templates/交接报告-template.md
  templates/任务简报-template.md
  templates/数据卡-template.md
  templates/数据卡-template-lite.md
  templates/文献卡-template.md
  templates/文献卡-template-lite.md
  templates/先行者清单-template.md
  templates/先行者清单-template-lite.md
  templates/README-模板拆分方案.md
  templates/status-template.md
  templates/status-template-lite.md
)

fail=0
for f in "${TEMPLATES[@]}"; do
  if [ -f "$f" ]; then
    # v2.2.x 残留模式（排除「v2.3.0 重命名」「→T」这种重构标注段）
    hits=$(grep -E 'T3 案例检索员|T7 终检|T8 批判|T5 审计|T5\.5' "$f" 2>/dev/null \
           | grep -v 'v2.3.0' | grep -v '→T' | grep -v '原 T' | grep -v '应改' | wc -l)
    if [ "$hits" -gt 0 ]; then
      echo "FAIL: $f 含真正 v2.2.x 残留 $hits 次"
      fail=$((fail+1))
    fi
  fi
done
[ "$fail" -eq 0 ] && echo "✅ 门 M 通过：13 模板无真正 v2.2.x 残留"
```

### 门 N：_shared/ 协议文档全量扫描（v2.3.0.1 新增，教训 #118 第 7 次连发）

> **背景**：v2.3.0 推送时 _shared/ 主目录的 4 个当前生效文件（通用韧化块 / 执行韧化协议 / M-Gate-渐进式验证 / failure-modes + audit-checklist-quickref）漏改 v2.2.x → v2.3.0 编号，主人在 11:46「整体检查所有文件」打脸才修。

```bash
SHARED_FILES=(
  _shared/通用韧化块-v2.1.0.md
  _shared/执行韧化协议-v2.1.0.md
  _shared/M-Gate-渐进式验证-v2.2.15.md
  _shared/M-Gate-Algorithm.md
  _shared/failure-modes.md
  _shared/audit-checklist-quickref.md
  _shared/版本升级自审门-v2.3.0.md
)
# v2.2.x 残留模式（每个文件 grep 都不应有命中）
LEGACY_PATTERNS=(
  'T3 案例检索员'
  'T7 终检'
  'T8 批判'
  'T5 审计'
  'T5\.5'
  'T4 分析员'
  'T5 写手'
)

fail=0
for f in "${SHARED_FILES[@]}"; do
  # 自审门-v2.3.0.md 自身作为元文档（描述 v2.2.2 历史背景 + 包含 LEGACY_PATTERNS 数组示例）应跳过
  if [ "$f" = "_shared/版本升级自审门-v2.3.0.md" ]; then continue; fi
  if [ -f "$f" ]; then
    for p in "${LEGACY_PATTERNS[@]}"; do
      # 排除「v2.3.0 重命名」「→T」这种重构标注段
      hits=$(grep -E "$p" "$f" 2>/dev/null | grep -v 'v2.3.0' | grep -v '→T' | grep -v '原 T' | wc -l)
      if [ "$hits" -gt 0 ]; then
        echo "FAIL: $f 含真正 v2.2.x 残留 '$p' $hits 次"
        fail=$((fail+1))
      fi
    done
  fi
done
[ "$fail" -eq 0 ] && echo "✅ 门 N 通过：6 _shared/ 文件无真正 v2.2.x 残留（元文档自审门跳过）"
```

### 门 O：_shared/ 子编号一致性（v2.3.0.1 新增）

> **背景**：v2.3.0 把 T5.5 → T7.5，T5.2 修订回环 → T7.2。子编号错位是「上层编号对了、子编号没改」的隐蔽坑。

```bash
# 检查当前生效 _shared/ 文件是否有真正 T5.5/T5.2 残留（排除 v2.3.0 标注段）
fail=0
for f in _shared/通用韧化块-v2.1.0.md _shared/执行韧化协议-v2.1.0.md \
         _shared/M-Gate-渐进式验证-v2.2.15.md _shared/M-Gate-Algorithm.md \
         _shared/failure-modes.md _shared/audit-checklist-quickref.md; do
  if [ -f "$f" ]; then
    hits=$(grep -E 'T5\.5|T5\.2' "$f" 2>/dev/null | grep -v 'v2.3.0' | grep -v '→T7' | grep -v '重命名' | wc -l)
    if [ "$hits" -gt 0 ]; then
      echo "FAIL: $f 含真正 T5.5/T5.2 残留"
      fail=$((fail+1))
    fi
  fi
done
[ "$fail" -eq 0 ] && echo "✅ 门 O 通过：6 _shared/ 文件无真正 T5.5/T5.2 残留"
```

### 门 P：_shared/ archive vs 当前分离（v2.3.0.1 新增）

> **背景**：archive/ + 版本升级自审门-v2.2.6.md 是历史快照，按教训 #91 保留原版；当前生效文件必须 v2.3.0 化。

```bash
HISTORY_FILES=(
  _shared/archive/M-Gate-Algorithm-legacy/M-Gate-Algorithm-v2.2.0.md
  _shared/archive/M-Gate-Algorithm-legacy/M-Gate-Algorithm-v2.2.1.md
  _shared/archive/M-Gate-Algorithm-legacy/M-Gate-Algorithm-v2.2.1.2.md
  _shared/archive/M-Gate-Algorithm-legacy/M-Gate-Algorithm-v2.2.4.md
  _shared/版本升级自审门-v2.2.6.md
)
CURRENT_FILES=(
  _shared/通用韧化块-v2.1.0.md
  _shared/执行韧化协议-v2.1.0.md
  _shared/M-Gate-渐进式验证-v2.2.15.md
  _shared/M-Gate-Algorithm.md
  _shared/failure-modes.md
  _shared/audit-checklist-quickref.md
  _shared/版本升级自审门-v2.3.0.md
)
echo "门 P 验证：历史快照 ${#HISTORY_FILES[@]} 个（保留 v2.2.x）+ 当前生效 ${#CURRENT_FILES[@]} 个（门 N/O 验证）"
echo "✅ 门 P 通过：历史/当前分离清晰"
```

### 门 Q：主控卡修订回环段 T 编号（v2.3.0.1 新增）

> **背景**：v2.3.0 推送时主控卡 §220-280 修订回环段（v2.2.0 P0-D 引入）漏改——T4 重写 / T5 审计打回等 v2.2.x 命名残留。

```bash
LEGACY_LOOP=(
  'T4 重写'
  'T5 审计打回'
  'T5 复核'
)
fail=0
for p in "${LEGACY_LOOP[@]}"; do
  hits=$(grep -cE "$p" references/agents/00-主控-coordinator.md 2>/dev/null)
  if [ "$hits" -gt 0 ]; then
    echo "FAIL: 主控卡含 v2.2.x 修订回环残留 '$p' $hits 次"
    fail=$((fail+1))
  fi
done
[ "$fail" -eq 0 ] && echo "✅ 门 Q 通过：主控卡修订回环段全用 v2.3.0 编号"
```

### 门 R：deliverables / glossary / 设计文档 元文档扫描（v2.3.0.1 新增）

> **背景**：v2.3.0 推送时 deliverables.md + 设计文档-哲学.md + glossary.md §四 等元文档漏改。

```bash
META_DOCS=(
  pipeline/deliverables.md
  pipeline/glossary.md
  pipeline/设计文档.md
  pipeline/设计文档-哲学.md
  pipeline/设计文档-架构.md
)
META_KEYWORDS=('v2.3.0' '原 T6→T3' '原 T8→T6' 'T8 主控亲完成')

fail=0
for f in "${META_DOCS[@]}"; do
  if [ -f "$f" ]; then
    cnt=0
    for kw in "${META_KEYWORDS[@]}"; do
      grep -qF "$kw" "$f" 2>/dev/null && cnt=$((cnt+1))
    done
    if [ "$cnt" -lt 3 ]; then
      echo "FAIL: $f v2.3.0 化不足（仅 $cnt/4 关键词命中）"
      fail=$((fail+1))
    fi
  fi
done
[ "$fail" -eq 0 ] && echo "✅ 门 R 通过：5 元文档全 v2.3.0 化"
```

### 门 S：scripts/ + CI 元元文档扫描（v2.3.0.2 新增，教训 #118.1）

> **背景**：v2.3.0 重构时 scripts/check-version.sh + 版本号同步脚本 的角色文件名漏改（仍是 v2.2.x 旧名），运行直接报错。这是「元元文档盲区」——17 门自审门只管 references/ 下文档，不管 scripts/ + CI。教训 #118.1。

```bash
# scripts/ + CI 里的角色文件名，必须与 references/agents/ 实际文件名一致
EXPECTED_AGENTS=$(ls references/agents/ | grep -E '^0[0-8]-' | grep -v '\.bak' | sort)
SCRIPT_AGENTS=$(grep -hoE 'agents/0[0-9]-[^"]*\.md' scripts/*.sh .github/workflows/*.yml 2>/dev/null | sed 's#agents/##' | sort -u)

fail=0
for a in $EXPECTED_AGENTS; do
  # 每个实际角色卡，都应在 scripts/CI 中被引用
  if ! echo "$SCRIPT_AGENTS" | grep -qF "$a"; then
    echo "FAIL: 角色卡 $a 未被 scripts/CI 引用"
    fail=$((fail+1))
  fi
done

# 反向：scripts/CI 引用的每个文件名，都应是实际存在的角色卡
for s in $SCRIPT_AGENTS; do
  if ! echo "$EXPECTED_AGENTS" | grep -qF "$s"; then
    echo "FAIL: scripts/CI 引用了不存在的角色卡 $s（v2.2.x 旧名？）"
    fail=$((fail+1))
  fi
done

[ "$fail" -eq 0 ] && echo "✅ 门 S 通过：scripts/CI 角色文件名与实际一致"
```

### 门 T：负向验证——组合残留双向扫描（v2.3.0.2 新增）

> **背景**：17 门自审门是「正向验证」（该有的都有），但缺「负向验证」（不该有的都没有）。perl 替换连带误伤（T1/T2/T4）+ 同一文件部分命中（F8 改了 F9 漏了）都是负向盲区。

```bash
# 对每个 v2.2.x → v2.3.0 映射，双向扫描：
#   正向：新编号在对应上下文出现 ≥1 次（已有门 H 覆盖）
#   负向：旧编号组合在非标注段出现 = 0 次（本门新增）

NEGATIVE_PATTERNS=(
  'T6 案例检索'    # 旧：案例检索是 T6 → 新 T3
  'T8 批判'       # 旧：批判伙伴是 T8 → 新 T6
  'T5 审计'       # 旧：审计是 T5 → 新 T7
  'T7 终检'       # 旧：终检是 T7 → 新 T8 主控亲完成
  'T3 分析'       # 旧：分析是 T3 → 新 T4
  'T5 写手'       # 旧：写手是 T4 → 新 T5
  'T1/T2/T6'      # 旧：三方并行 T1∥T2∥T6 → 新 T1∥T2∥T3
  'T1/T2/T4'      # perl 误伤产生的错误组合
  'T1∥T2∥T6'      # 旧：三方并行符号
  'T5\.5'         # 旧：子编号 → 新 T7.5
  'T5\.2'         # 旧：子编号 → 新 T7.2
)

# 扫描范围：references/ 全量（排除 archive + .bak + 版本历史段）
fail=0
while IFS= read -r file; do
  for p in "${NEGATIVE_PATTERNS[@]}"; do
    hits=$(grep -E "$p" "$file" 2>/dev/null \
           | grep -v 'v2.3.0' | grep -v '→T' | grep -v '原 T' | grep -v '应改' | grep -v '重命名' | wc -l)
    if [ "$hits" -gt 0 ]; then
      echo "FAIL: $file 含 v2.2.x 组合残留 '$p' ($hits 处)"
      fail=$((fail+1))
    fi
  done
done < <(find references/ -name '*.md' | grep -v '\.bak' | grep -v 'archive')

[ "$fail" -eq 0 ] && echo "✅ 门 T 通过：无 v2.2.x 组合残留"
```

### 门 U：模型健康度预检（v2.3.1 新增，教训 #119）

> **背景**：v2.3.0 首次实战（ai-pilled vs agi-pilled）T7 审计员用 claude-opus-5 派发后 2m34s 返回 0 tokens（kkaiapi 接口静默无响应），T7 卡死，最后 fallback 到 deepseek-v4-pro 兑底才完成。根因：v2.3.0 配置里 T7 写死 claude-opus-5，无显式模型 fallback 链——这是设计漏洞，不是临时事故。

```bash
# 检查 T7 审计员卡 + pipeline-readme 是否显式声明 claude-opus-5 fallback 链 + 预检 + 标注三件套
T7_CARD="references/agents/07-审计-auditor.md"
PR="pipeline/pipeline-readme.md"

fail=0
# 1. T7 卡显式声明主模型 claude-opus-5 + fallback 链
grep -q "claude-opus-5" "$T7_CARD" || { echo "FAIL: T7 卡缺 claude-opus-5 主模型声明"; fail=$((fail+1)); }
grep -q "deepseek-v4-pro → minimax-M3" "$T7_CARD" || { echo "FAIL: T7 卡缺专属 fallback 链"; fail=$((fail+1)); }

# 2. pipeline-readme 含 T7 派发前 1-token ping 预检机制 + 教训 #119 标注
grep -q "1-token ping" "$PR" || { echo "FAIL: pipeline-readme 缺 1-token ping 预检"; fail=$((fail+1)); }
grep -q "教训 #119" "$PR" || { echo "FAIL: pipeline-readme 缺教训 #119 标注"; fail=$((fail+1)); }

# 3. fallback 触发标注机制（主模型静默无响应 → 兑底标注）
grep -q "静默无响应" "$T7_CARD" || { echo "FAIL: T7 卡缺 fallback 触发标注机制"; fail=$((fail+1)); }

[ "$fail" -eq 0 ] && echo "✅ 门 U 通过：T7 模型 fallback 链 + 预检 + 标注三件套齐全"
```

## 判定与失败处理

```
20 门全过（exit 0）→ 允许 commit + tag + push
任一门 FAIL → 禁止 commit，回到对应真源层补同步，重新跑全部门
自审门报告写入 final/版本升级自审-vX.md（或 audits/），随 commit 一并记录
```

**失败处理优先级**：
- 门 B（角色三处一致性）失败 → 补派发话术/全景/目录三处，1 轮内可关
- 门 D（双端 md5）失败 → cp 同步副本，立即关
- 门 C/E（版本号/五层）失败 → 补对应真源层，通常需主人确认（GitHub/DSH 本地记忆 是公开动作）
- **门 N/O/P/Q/R（_shared/ 元文档扫描）**失败 → 教训 #118 主线，论衡 v2.3.0 推送时漏改 12 处（11:46 主人打脸），这些门是「防 5 层真源外元文档漏改」的护栏
- **门 S/T（元元文档 + 负向验证）**失败 → 教训 #118.1，14:03 第三方独立审计发现的 1 P0 + 7 P1 漏改（scripts 文件名 + deliverables F9 + 设计文档 Phase 1.5 + 模板 + 实战案例库）
- **门 U（模型健康度预检）**失败 → 教训 #119，v2.3.0 首次实战 T7 claude-opus-5 静默无响应；补 T7 卡 fallback 链 + pipeline-readme 预检 + 标注三件套

## 论衡哲学化

- 「**版本升级也需要 M 门**」——终稿有 M 门查成品，版本升级也要有门查机制本身，两层都不能靠自觉。
- 「**改 A 漏 B 是 LLM 的结构性通病，不是偶然失误**」——教训 #36/#58/#60 + v2.2.6 T8 漏同步四次同源，必须用机械化 grep 兜底，不能靠「下次注意」。
- 「**自审门是论衡的「自指」**」——论衡用审别人的标准审自己，这正是「独立角色 + 机械化门 > 自我复核」命题的最高形态。

## 与终稿 M 门的区别

| 维度 | 终稿 M 门 | 版本升级自审门 |
|------|----------|--------------|
| 对象 | 论文成品（final/定稿.md） | 论衡机制本身（角色卡/文档/真源） |
| 触发 | T8 终检（v2.3.0 改 T7→T8） | 每次版本升级 commit 前 |
| 检查 | M-Form 6 + M-Exist 3 | 角色清单一致性 + 双端 md5 + 版本号 + 真源五层 |
| 产出 | M-Gate-Report-v2.2.4.json | 版本升级自审-vX.md |
