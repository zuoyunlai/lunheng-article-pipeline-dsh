## ⚡ 执行韧化协议（v2.1.0 必读，不接受则不启动）

> **DSH 精简说明（v2.3.7-dsh.1）**：DSH 下仅执行精简版——状态机 + 交接报告六要素 + G8 自检 + 超时介入（`list_agents`），心跳/ack/预检/硬卡不强制（见 `执行韧化协议-v2.1.0.md` 头部说明）。

> ⚠️ **已内联存档**（v2.1.0 起本协议已内联到每张角色卡顶部「执行韧化协议」段，本文件为历史存档，不再独立维护）

> 这是论衡 v2.1.0 激进重构加入的协议。每个角色启动必须遵循，违背会拖垮整个流水线。

1. **启动心跳**（30 秒内必做）：更新 `status.md` 对应行为 `🔄 In Progress` + 写明「启动时间 + 当前模型」。不写心跳 = 主控视为未启动。
2. **分阶段 ack**（按任务时长分级）：
   - **<2 分钟**：启动 ack 即可，无需中间 ack
   - **2-5 分钟**：启动 + 完成二段 ack
   - **5-15 分钟**：启动 + 25% + 50% + 75% + 完成 五段 ack
   - **>15 分钟**：**禁止**——必须拆任务
3. **模型健康度预检**：第一次 LLM 调用前先发 1-token ping（"ok"），30 秒无响应 → 降级到论衡 model.fallbacks 链（minimax-portal/MiniMax-M3 → deepseek/deepseek-v4-flash → coding-plan/glm-5.3）。降级日志写 status.md：`[降级 HH:MM] primary→fallback1, 原因=ping超时`。
4. **超时硬卡（分角色分级，v2.3.2 放宽，教训 #126 + #131）**：T1-T3 检索员 10-12 分钟；T6 批判/T7 审计 12-15 分钟；T4/T5 保持 8 分钟。提前 2 分钟写警告，提前 1 分钟必须产出 partial output，到点被主控 kill 标记 Failed。
5. **禁止假装在线**：ack 必须真实反映进度，禁止写「完成 X 节」但实际未做。
6. **不能进入 G8 审计负面**：你产出的 final/* 材料要经 **T7** 审 G8 成品度（v2.3.0 改 T5→T7）——完成后**自检**一遍：grep "v2 稿/初稿/草稿/修订说明/主人结构性观察/据行业经验估算/论据类型"，有任何命中立即删除。