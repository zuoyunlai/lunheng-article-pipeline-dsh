# 机制文件修订记录 — 2026-09-26（v18.19.0 二次复审方案 M 族落地批 · v18.20.4）

> **授权依据**：本次机制文件改动**依据主人显式授权**（原话「版本号不管它，你先核实这份文件的问题，如果还存在需要修订的，制定方案开始修订」，对应 `AGENTS.md` §机制文件写保护 的唯一例外条款）。
> **配套方案**：`audits/反哺报告-v18.19.0-二次复审修订方案.md`（M-1～M-6）。
> **交付提交**：`b7b6747`（M-1/M-2）+ `647be17`（M-3/M-4/M-5）+ 本批归版提交（v18.20.4）。
> **备份位置**：`<DSH_HOME>/_backup/lunheng-2026-09-26-v1819/`（180 文件：scripts/ + tests/ + skills/ + lib/ + package.json + cordis.patch.yml + .github/）。
> **回滚**：`git revert b7b6747 647be17 <本批>` 或从备份目录恢复。

## 一、核实结论（逐项受控复现，不凭方案自述）

| 项 | 方案主张 | 复现结果 |
|---|---|---|
| M-1 | 发布物集 ≠ 扫描集（gitignored `.bak` 进包却不扫） | ✅ **真实现存**：注入含 `sk-…` 的 `.bak` → ⑦ 报「无命中」但 `npm pack` 收录 |
| M-2 | npm 12 形态 → 解析抛错 + ⑦b 真空合格 + 3 测试静默 pass | ✅ **真实现存（潜伏）**：node 模拟对象形态必抛；`catch { return }` 吞成 pass |
| M-3 | docs-facts 对 5 个随包文档无对账 | ⚠️ **复发风险**（4/5 文档不提工具，非现行缺陷）；扩面后抓出 4 处真实缺口 |
| M-4 | §8 表格无门 | ✅ **真实现存**：表内 `2`→`12` 时 Gate 全绿 |
| M-5 | `renameSync` 无重试 | ✅ **真实现存**：`destructive-write.mjs` 裸调用 |
| M-6 | 发布纪律 | ⚠️ **无需改动**：`publish.yml` gates 已跑全量测试（含 docs-facts） |

## 二、机制文件改动

| 文件 | 改动 | 性质 |
|---|---|---|
| `scripts/_lib/pack-manifest.mjs` | **新增**：`npm pack --json` 单点解析器（数组 ≤npm11 / 对象 npm12+；形状不认识抛 `PackManifestShapeError`）+ `isPackEnvUnavailable()` | 仓库级脚本（不随包） |
| `scripts/repo-hygiene-check.mjs` | ⑥ 改用单点解析；`packFiles` 改三态；⑦b UNKNOWN 不打印合格字样；④⑤⑦ 改扫 `scanSet`；**新增 ⑥b 差集门**；⑧b 增 §8 表格对账 | 仓库级脚本 |
| `scripts/_lib/exit-namespace.mjs` | 新增 `parseExitTable`（§8 表格码列解析） | 仓库级脚本 |
| `skills/lunheng-article-pipeline/scripts/_lib/destructive-write.mjs` | 新增 `renameWithRetry`（句柄类瞬时错误有界重试 3 次） | 随包脚本（**镜像已同步**） |

## 三、非机制文件改动

- `package.json`：`files` 加 8 条负向项（`*.bak*`/`*.tgz`/`.env*`/`*.pem`/`*.key`/`*.p12`/`*.pfx`/`.credentials*`）。
- `tests/docs-facts.test.mjs`：`SURFACES` 从 pack 清单派生 + 逐面参与度 + 跳过版本演进史行；C-5 改单点解析。
- `tests/local-path-scan.test.mjs` / `tests/pack-negative.test.mjs`：改单点解析 + catch 按错误类型分流。
- `tests/second-review-gates.test.mjs`：**新增** 6 用例。
- `docs/troubleshooting.md`（§8 表补 `| 4 |` 行 + 技能名 + `/lunheng-stats`）、`docs/architecture.md`、`docs/faq.md`：补子技能名（M-3 派生扩面抓出的真实缺口）。
- `CHANGELOG.md`：`## 18.20.4` 段。

## 四、验证

四道具全绿（consistency 0 漂移 / plugin-surface 9 过 0 失败 / repo-hygiene 全过 / pack-smoke 通过）+ 全量测试通过（352 基线 + 6 新增）。M-1～M-4 均附**反向自证**。

## 五、过程教训（写入记忆）

**反向自证用 `git checkout <file>` 还原时，会把该文件**所有**未提交改动一并还原。** 本批两次踩到：M-4 自证还原了 `troubleshooting.md` 的 `| 4 |` 行、M-3 自证还原了 `faq.md` 的子技能名补丁——前者已补回，后者直到全量测试报红（`docs/faq.md 全文未提及技能 lunheng-commands`）才发现。**正确做法**：注入自证要么先提交修复、要么用「备份副本注入」而非就地注入 + `git checkout` 还原。
