# 论衡机制文件修订记录 — 2026-09-25（全量审计第一梯队）

> **授权依据**：主人直接指令「开始修订」——依 `AGENTS.md` §机制文件写保护「**唯一例外**：主人显式授权」条款执行。
> 本条如实标注：**本次机制文件改动依据主人显式授权**（默认约束为「agent 自发改进 = 禁写」，本次突破系主人明确下令）。
> **提交**：`0a10f98`（fix(v18.12.0)）
> **上游依据**：`audits/全量审计报告-v18.11.0.md`（本次审计，49+ 条编号发现；本批只做**第一梯队** 6 项）

---

## 一、改前备份（可回滚）

| 项 | 值 |
|---|---|
| 备份位置 | `C:\Users\Zuoyunlai\.dsh\_backup\lunheng-cgaudit-20260925\`（**工作区外**，符合 AGENTS.md ①） |
| 备份内容 | `SKILL.md` + `AGENTS.md` + `scripts/`（42 个 .mjs）+ `references/`（70 个文件）＝ **114 个文件** |
| 行数基线 | 见 §三 对照表（改前 → 改后） |

**整体回滚命令**（PowerShell，任选其一）：

```powershell
# ① git 回滚到修订前（推荐：本次改动已提交，工作树干净）
cd E:\HERNESS\lunheng-article-pipeline-dsh
git revert --no-edit 0a10f98

# ② 或从备份硬还原（备份是改前快照）
$b = 'C:\Users\Zuoyunlai\.dsh\_backup\lunheng-cgaudit-20260925'
$s = 'E:\HERNESS\lunheng-article-pipeline-dsh\skills\lunheng-article-pipeline'
Copy-Item "$b\SKILL.md","$b\AGENTS.md" $s -Force
Copy-Item "$b\scripts\*" "$s\scripts\" -Recurse -Force
Copy-Item "$b\references\*" "$s\references\" -Recurse -Force

# ③ 同步回部署镜像（镜像与真源必须一致，否则 consistency-check 判 P1 漂移）
$m = 'E:\HERNESS\.dsh\skills\lunheng-article-pipeline'
Copy-Item "$s\SKILL.md","$s\AGENTS.md" $m -Force
Copy-Item "$s\scripts\*" "$m\scripts\" -Recurse -Force
Copy-Item "$s\references\*" "$m\references\" -Recurse -Force
```

**注意**：仓库级脚本 `scripts/repo-hygiene-check.mjs`（棘轮抬升）**不在**上述备份内（它不属技能目录）；回滚它须用 `git revert`（路径 ①）。

---

## 二、本批改了什么（6 项，均为审计 P0）

| 审计 ID | 问题 | 修法（落点） | 验证 |
|---|---|---|---|
| **L-50** | `--report` 无同文件守卫 → 可**静默销毁被审正文**（8 个脚本同型；实测 `apply-diff … --report a.md` → exit 0、a.md 被 JSON 覆盖、无 `.bak`） | 新增 `_lib/destructive-write.mjs` 的 **`writeReport()`** 唯一实现（同文件 → 打印 + **exit 10**；通过则走 `writeWithSafety(..., {inPlace:true})` 留时间戳 `.bak`）；8 个调用点改用：`apply-diff` / `m-gate-check` / `final-check` / `cite-coverage-check` / `structure-check` / `methodology-check` / `journal-fit` / `meta-synthesize`；同时移除各脚本已不再使用的 `writeFileSync` 导入 | **黑盒实测**：apply-diff 与 m-gate-check 的 `--report <被审文件>` → **exit 10 且源文件 sha256 逐字节不变、零回滚点**；`--report` 指向合法路径 → exit 0 正常落盘；`..` 归一与大小写绕过 → 均 exit 10 |
| **L-51** | 正文区边界**不识别围栏代码块** → 正文里被引用的 `## 参考文献` 被当真文末节起点 → `count-chars`（唯一字数验收口径）静默偏小（实测 15 vs 手算 31）、exit 0、无 degraded | `_lib/sections.mjs` 新增 **`maskFences()`**（把围栏区间替换为**等长空格**，偏移与行结构逐字节不变）；`h2Headings` / `h3Headings` 改在遮罩文本上匹配 → 全部下游自动生效（count-chars / m-gate-check / segment-chars / fix-gates / mform-gates / mintegrity-gate / cite-coverage-check …） | 新增 `tests/sections-fence.test.mjs`（9 例）：保长 / 换行数 / 围栏内 `##` 不算标题 / 跳过伪文末节 / 未闭合围栏 / 波浪围栏 / ```` ``` ```` 与 ```` ```` ```` 嵌套 / **count-chars 端到端字数=独立手算值** |
| **L-52** | 模板明文的「围栏内索引段」抢先命中 `splitCard` 的行内式 → **完全合规**的数据卡被 M-Form-6 判 P0（>5 条必触发）；连带 `normalize-trust-level` 插入点恒在条目前（非幂等） | `_lib/cards.mjs`：匹配前遮罩围栏 + 多候选时**优先「像真条目」者**（含 `信任级别` 或 `├/└/│`）；`normalize-trust-level.mjs` 插入点改 `indexOf('\n', 1)`（跳过行内式块首换行） | 新增 `tests/cards-split.test.mjs`（4 例）：模板形态命中**条目**而非索引段 / 标题式仍优先 / 真缺信任级别时仍由门报 P0（不得静默回退成索引行）/ **normalize 连跑两轮幂等且插入点在条目内** |
| **L-17** | 文末节数「写手卡要九节 vs 机检只认五节」→ 学术稿**照规范写必被 M-Form-7 判 P0**，而该门属不可兜底红线 → **无出口** | `sections.mjs` 拆两层：`ENDNOTE_SECTIONS`（**必需五节**，M-Form-2 存在性）+ `ENDNOTE_SECTIONS_OPTIONAL`（学术四声明）+ **`ENDNOTE_ORDER`**（九节合规顺序，由必需五节派生）；`mform-gates.mjs` 的 `WHITELIST` 改用 `ENDNOTE_ORDER`、M-Form-2 仍用必需五节；同步 `deliverables.md`（白名单表 + 必需列）/ `M-Gate-Algorithm.md`（伪代码两处）/ `audit-checklist-quickref.md` / `05-写作-writer.md`（§铁律 9 + §四声明义务）/ `交付说明-template.md` | 新增 `tests/endnote-nine.test.mjs`（5 例）：**九节 → M-Form-2/M-Form-7 双通过**（旧版此处 P0）/ **只写五节 → 也通过**（非学术稿不得被强制要四声明）/ 四声明错序 → M-Form-7 仍报 P1 / 白名单外节（`## 合规报告`）→ 仍判 **P0** |
| **L-20** | `M-Form-12` **幻影门号**：四份学术声明模板 + 写手卡声称「T7 跑 M-Form-12 子门核验档位」，而 M 门编号体系内**不存在该门** → **假绿**（写手据此以为有机检兜底） | 四份声明模板（CRediT / COI / 数据可用性 / 伦理审批）+ 写手卡自检三连 + 交付说明 §11.1 全部改判「**T7 人工核验（无脚本机检）**」；`methodology-check.mjs` 的虚假「与 m-gate-check 同源维护」声明更正为「**自有命名空间**」；`规范-机械门对照表.md` 新增 3 行登记（含 `M-Exist-11` 同号两义） | 全库 grep：不再有「跑 M-Form-12 子门」的声称；对照表三行可逐条核 |
| **L-18** | 轻量档「T4 大纲可省」→ `分析大纲.md` 无产出者，而 M-Exist-10 以它为对象 → 轻量档 **T7.5 必然非 0**，只能走 Acknowledged Limitations | `SKILL.md` §边界与轻量化建议 + `pipeline-readme.md` 补**连带规则**：省略 T4 时二选一 —— ① 主控代产最小 §11 精简段；② 在 `status.md` + `final/局限性.md` 显式记「省略 T4 → M-Exist-10 记 N/A」并把该门计入 T7.5 **豁免清单**（不因它非 0 判整档不合格） | 文档级；机检落点（M-Exist-10 的 N/A 文案）已在报告中给出后续建议 |

---

## 三、改动清单与行数对照

| 文件 | 改前 → 改后 | 性质 |
|---|---|---|
| `scripts/_lib/destructive-write.mjs` | 165 → 198 | 新增 `writeReport()`（+33） |
| `scripts/_lib/sections.mjs` | 97 → 166 | 新增 `maskFences()` / 四声明 / `ENDNOTE_ORDER`；H2/H3 走遮罩（+69） |
| `scripts/_lib/cards.mjs` | 37 → 57 | 围栏遮罩 + 候选择真（+20） |
| `scripts/_lib/mgate-gates/mform-gates.mjs` | 955 → 959 | 白名单改九节 / 存在性仍五节（+4） |
| `scripts/{apply-diff,m-gate-check,final-check}.mjs` | — | `--report` 改走 `writeReport` |
| `scripts/{cite-coverage-check,structure-check,methodology-check,journal-fit,meta-synthesize}.mjs` | — | 同上 |
| `scripts/normalize-trust-level.mjs` | 97 → 99 | 插入点 `indexOf('\n', 1)`（+2） |
| `SKILL.md` | 226 → 235 | 轻量档连带规则（+9） |
| `references/deliverables.md` | 169 → 180 | 文末白名单九节两层表（+11） |
| `references/_shared/M-Gate-Algorithm.md` | 1161 → 1171 | M-Form-2/7 伪代码口径（+10） |
| `references/_shared/audit-checklist-quickref.md` | 144 → 144 | G4④ 行口径改写（±0） |
| `references/_shared/规范-机械门对照表.md` | 119 → 123 | 登记 3 行（+4） |
| `references/agents/05-写作-writer.md` | 431 → 435 | §铁律 9 + 自检三连（+4） |
| `references/pipeline-readme.md` | 824 → 828 | T4 连带规则（+4） |
| `references/templates/{CRediT,COI,数据可用性,伦理审批}*.md` | — | 幻影门声明改判（各 ±1 行） |
| `references/templates/交付说明-template.md` | 132 → 133 | §11.1 口径（+1） |
| `scripts/repo-hygiene-check.mjs`（**仓库级**） | 542 → 542 | 词预算棘轮抬升 3 处（见 §四） |
| `tests/sections-fence.test.mjs`（新） | — | 9 例 |
| `tests/cards-split.test.mjs`（新） | — | 4 例 |
| `tests/endnote-nine.test.mjs`（新） | — | 5 例 |
| `audits/全量审计报告-v18.11.0.md`（新） | — | 788 行审计报告 |

合计 `30 files changed, 1349 insertions(+), 57 deletions(-)`。

---

## 四、按 AGENTS.md 抬升的棘轮（3 处，理由已写入 `DOC_BUDGET`）

| 文档 | 上限 | 抬升理由（摘要） |
|---|---|---|
| `references/agents/05-写作-writer.md` | 55 → **56 KB** | §铁律 9 改写为「必需五节 + 可选四声明」两层并补机检真源指针；自检三连更正「五节扩面到七节」错标与「M-Form-12 子门」假绿 |
| `references/deliverables.md` | 17 → **18 KB** | 「定稿文末白名单」由「只允许 5 节」改写为必需/可选两层表 + 真源指针（旧表述与 v18.10.0 起写手卡要求的九节互斥） |
| `references/_shared/规范-机械门对照表.md` | 24 → **26 KB** | 新增三行勾稽：文末九节 / 四声明档位（无门 → 人工）/ `methodology-check` 命名空间撞号登记 |

> 其余增长的文档均**未超各自上限**，故未抬升。**注意**：`M-Gate-Algorithm.md` 抬升后**余量仅 44 B**、`deliverables.md` 余量 61 B —— 下批若再动这两份文档，须**先瘦身**或再次同提交抬棘轮。

---

## 五、改后验证（四道门全绿）

| 门 | 结果 |
|---|---|
| `node --test "tests/**/*.test.mjs"` | **exit 0 / 255 例全通过**（原 237 + 本批新增 18） |
| `node skills/lunheng-article-pipeline/scripts/consistency-check.mjs` | **exit 0** —— 74 个 .md + patch/examples/.dsh 同步，0 处漂移 |
| `node scripts/repo-hygiene-check.mjs` | **exit 0** —— 10 条规则全通过（含 ⑨ 词预算 / ⑩ 注解密度） |
| `node scripts/plugin-surface-check.mjs` | **exit 0** —— 9 通过 / 0 失败 / 1 提示（不阻塞） |

**黑盒验证（L-50 专项，夹具在 `$env:TEMP`，未污染仓库）**：

```
apply-diff a.md list.md --out b.md --report a.md   → exit 10，a.md sha256 D7515476… 逐字节不变，0 个 .bak
m-gate-check a.md <ev> --report a.md               → exit 10，a.md 逐字节不变
apply-diff a.md list.md --out b2.md --report r.json → exit 0，r.json 正常落盘（720 B）
--report <…\..\lh-rev-L50\a.md>（.. 归一）          → exit 10
--report <…\A.MD>（大小写）                          → exit 10
```

**镜像同步**：`E:\HERNESS\.dsh\skills\lunheng-article-pipeline`（117 文件）已按 AGENTS.md §4「`cp` 不带转换直接覆盖」同步，且**无污染**（无 `package.json` / `cordis.patch.yml` / `docs` / `examples` / `lib` / `.git`）；同步后复跑 consistency-check 仍 **exit 0**。

---

## 六、本批**未**做的事（如实声明）

1. **版本号未 bump**：`package.json` 仍为 `18.11.0`，而代码注释与文档标注为 `v18.12.0`。版本级联 bump（SKILL frontmatter `version:` + description 首版本号 / 五语 README pin / `cordis.patch.yml` 包头 / CHANGELOG 段 / docs 当前版本 / 各脚本 `version:` 契约）是**发布步**，按仓库既有流程与 tag 同步进行——留给发布提交一并做，本批不做半截 bump（半截会让 `docs-version-rules` 判红）。
2. **第二梯队未动**（本次审计的最高价值项）：M-Exist-5 的五条复合空洞（L-54 模板项解析被 `break` 中断 / L-22 实据只做语法匹配 / L-03 结论词不与实据对账 / L-02 对账守卫要求全行皆 ✓ / L-10 必写的 `handoff-check` 码不查）、F-1 硬红线未实装（L-44）、T8 裁定正式入口（L-05）、`exit=3` 合规出口（L-45）、空集/缺文件静默通过（L-19 / L-23 / L-35）。
3. **第三梯队未动**：B 轨「不限额 vs +1」（L-21）、「轮」的定义收口（L-01）、锚点门扩面 + `pipeline-readme` 目录重写（L-24）、`final-check` 头注释（L-30）、写死模型名（L-31）、G 清单与 gate 标签（L-28）、`M-Form-8` 段上限（L-46）、`M-Exist-5` 指纹互锁（L-47）、`REPO_ROOT` 镜像自比（L-53）、`--fix --write` 写盘安全（L-66）、`cli-args` 收敛（L-62）等。
4. **未做发布面操作**：未 `npm publish`、未打 tag、未改 CHANGELOG。

**下一批建议起点**：L-54（先让 M-Exist-5 的逐项检查真的跑起来）→ L-22 / L-03 / L-02 / L-10（同批修，否则单独修一条这扇门仍无效）→ L-44（把 F-1 红线写成真代码）。
