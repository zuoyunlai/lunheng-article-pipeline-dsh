# 机制文件修订记录 — 2026-09-26（三次复审 N 族落地批 · v18.21.0）

> **授权依据**：本次机制文件改动**依据主人显式授权**（原话「继续修订」，对应 `AGENTS.md` §机制文件写保护 的唯一例外条款）。
> **配套方案**：`audits/反哺报告-v18.21.0-三次复审（DSH 宿主契约）.md`（N-1～N-4；**该原件按主人约定不提交 git**）。
> **交付提交**：`e31e240`（N 族修订）+ 本批归版提交（v18.21.0）。
> **备份位置**：`<DSH_HOME>/_backup/lunheng-2026-09-26-v1821/`（203 文件：scripts/ + tests/ + skills/ + lib/ + docs/ + .github/ + package.json + cordis.patch.yml + CHANGELOG.md）。
> **回滚**：`git revert e31e240 <本批>` 或从备份目录恢复。

## 一、核实结论（逐项受控复现）

| 项 | 级别 | 复现结果 |
|---|---|---|
| N-1 | **P1** | ✅ **真实现存**：隔离 `DSH_HOME` 逐字照抄文档第 1 步 → 装到 **18.15.0**（`latest` = 18.20.4）；lockfile 记 `specifier: ^18.15.0`；根因在**本机 pnpm 解析状态**（两套元数据命名空间，其一年久；但 18.15.0 与两者都对不上 → 与报告「未定位到 pnpm 内部规则」一致） |
| N-2 | P2 | ✅ **真实现存**：`--dump-config` 在 `LUNHENG_TIERING` 未设/on/off **三取值下都命中 3 行** |
| N-3 | P2 | ✅ **真实现存**：`ci.yml:131` = `@0.3.16` vs `plugin-surface-check.mjs:61` = `@0.3.19` |
| N-4 | P2 | ✅ **真实现存**：peer `>=0.1.2-rc.1` vs CI 只装 `0.1.7-rc.2` |

## 二、机制文件改动

| 文件 | 改动 | 性质 |
|---|---|---|
| `scripts/repo-hygiene-check.mjs` | **新增规则 ⑭「外部 CLI pin 单点」**（运行面只允许一处 `dsh-plugin-guide@<semver>`，第二处即红；刻意不扫 CHANGELOG/audits/docs 的留痕叙述）+ header 补 ⑥b/⑭ 两行 | 仓库级机制脚本 |
| `scripts/plugin-surface-check.mjs` | `CLI_SPEC` 改 `export` + 新增 `--print-cli-spec`（只打印不跑门，供 CI 单点派生） | 仓库级脚本 |

## 三、非机制文件改动

- `.github/workflows/ci.yml`：loader-smoke 改用 `--print-cli-spec` 派生（删掉硬编码 `@0.3.16`）。
- `docs/installation.md`（随包 → 须发版）：第 1 步钉版本 + 新增 1b) 版本核对命令 + §验证 改写（删误导措辞、增「不反映装载态/三取值不可区分」） + 新增前提第 4 条（N-4 的宿主已知缺陷与规避）。
- `lib/index.js`：新增 `readPackageVersion()` + **启动状态行带版本**（报告判定入口非机制文件，但建议同批 review）。
- `tests/third-review-gates.test.mjs`：**新增** 5 用例（N-1③ 为行为断言）。
- `CHANGELOG.md`：`## 18.21.0` 段。

## 四、验证

四道具全绿（consistency 0 漂移 / plugin-surface 9 过 0 失败 / repo-hygiene 全过**含新规则 ⑭** / pack-smoke 通过）+ 全量测试通过（358 基线 + 5 新增 = 363）。
**反向自证三条**：去掉版本自证行 → N-1③ 用例红；反转免责措辞 → N-2 用例红；注入第二处 pin → 规则 ⑭ 红（用「新建探针文件 + 删除」而非 `git checkout`，规避上次的还原坑）。

## 五、未做 / 待定案（明确边界）

- **N-4 的另一半**：CI 加最低宿主版本 leg ↔ 收紧 `peerDependencies` 声明 —— **二选一待主人定案**（三次复审 §三.3 定案 1）。本批只落了文档那一半（写明已知缺陷与规避）。
- **真实会话级验证**（两技能是否出现在会话目录 / 三档工具数是否真少 3）：需模型凭据，报告已按边界 4 弃权，本批同样不做。
- **`docs/installation.md` 的更正须随本版发版**才对已发布物生效（随包文件边界）。
